/* sysdesign.js — system-design simulator exercises.
   Base step models hold universally sound answers; each exercise overrides the
   scenario-specific steps. kp = key-point groups (label + match keywords). */
(function (D) {
  'use strict';

  var BASE = {
    clarify: { dims: ['communication'], kp: [['Functional scope and out-of-scope', 'scope', 'out of scope'], ['NFR numbers requested (volume, latency, availability)', 'volume', 'latency', 'availability', 'how many'], ['Constraints asked (regulation, deadlines, standards)', 'constraint', 'regulat', 'deadline'], ['What must never happen', 'never', 'must not']],
      model: 'Ask for functional scope and explicit out-of-scope, the NFR numbers (volumes, peaks, latency percentiles, availability, retention), constraints (regulation, mandated technology, deadlines), and the negative space: what must never happen (loss, duplication, leaks).' },
    stakeholders: { dims: ['communication'], kp: [['Users and business owner', 'user', 'business'], ['Operations and security/compliance', 'operations', 'compliance', 'security'], ['Downstream consumers', 'downstream', 'consumer']],
      model: 'End users, the business owner, operations (who runs it), security and compliance (controls, evidence), data protection, and downstream system owners who consume outputs.' },
    assumptions: { dims: ['communication'], kp: [['Traffic/growth assumptions stated', 'assume', 'traffic', 'growth'], ['Assumptions flagged for validation', 'validate', 'confirm'], ['Team/platform assumptions', 'team', 'platform']],
      model: 'State traffic and growth assumptions with numbers, team and platform maturity assumptions, and flag each for validation — "I assume X; I would validate it" is the professional posture.' },
    scale: { dims: ['architecture'], kp: [['RPS derived from volumes', 'rps', 'per second', 'peak'], ['Storage growth estimated', 'storage', 'tb', 'gb'], ['Peak factor applied', 'peak', 'factor', 'salary', 'month-end']],
      model: 'Derive average and peak RPS from daily volumes (with an explicit peak factor), estimate storage growth against retention, and let the arithmetic drive component choices.' },
    entities: { dims: ['domain', 'data'], kp: [['Core entities with relationships', 'entit', 'relationship', 'model'], ['Ownership/system-of-record noted', 'owner', 'system of record', 'master'], ['Lifecycle/state identified', 'lifecycle', 'state', 'status']],
      model: 'Name the core entities, their relationships and lifecycles (status fields imply state machines), and assign each entity a single system of record.' },
    apis: { dims: ['architecture'], kp: [['Key operations with idempotency', 'idempoten', 'post', 'api'], ['Error and status behaviour', 'error', 'status', '4xx'], ['Pagination/versioning where relevant', 'pagination', 'version']],
      model: 'Define the key operations with idempotency keys on unsafe methods, a consistent error model with correlation IDs, pagination on collections, and a versioning policy.' },
    events: { dims: ['kafka'], kp: [['Events named as past-tense facts', 'event', 'past', 'created', 'completed'], ['Keys chosen by ordering scope', 'key', 'order', 'partition'], ['Producer ownership stated', 'owner', 'producer'], ['Outbox for reliable publication', 'outbox']],
      model: 'Publish past-tense domain facts keyed by the entity whose order consumers need, owned by their producing domain, and emitted via transactional outbox so state and event share fate.' },
    storage: { dims: ['data'], kp: [['Store per data shape with reasons', 'relational', 'store', 'because'], ['SoR vs derived distinction', 'derived', 'read model', 'rebuild'], ['Retention/archiving noted', 'retention', 'archive']],
      model: 'Relational for the transactional system of record; derived, rebuildable read models (search, cache) where query shapes demand; retention and archiving planned per data class.' },
    hla: { dims: ['architecture'], kp: [['Containers with responsibilities', 'service', 'container', 'responsib'], ['Sync vs async flows chosen', 'sync', 'async', 'event'], ['Edge/gateway present', 'gateway', 'edge']],
      model: 'A container-level description: gateway at the edge, services per bounded capability, sync calls only where the caller needs the answer, events for propagation, stores owned per service.' },
    boundaries: { dims: ['architecture', 'domain'], kp: [['Boundaries from bounded contexts/language', 'bounded context', 'language', 'domain'], ['Transaction test applied', 'transaction', 'saga', 'span'], ['Team/ownership alignment', 'team', 'conway', 'own']],
      model: 'Boundaries follow bounded contexts (language shifts), checked against the transaction test (operations spanning boundaries transactionally mean a wrong cut) and team ownership.' },
    deepdive: { dims: ['architecture'], kp: [['Riskiest component identified', 'riskiest', 'critical', 'hardest'], ['Internal design with failure handling', 'timeout', 'retry', 'state'], ['Concurrency/idempotency addressed', 'concurren', 'idempoten', 'race']],
      model: 'Pick the riskiest component, design its state machine and concurrency handling (optimistic versions, idempotent operations), and walk its failure behaviour per dependency.' },
    security: { dims: ['security'], kp: [['AuthN/Z model (OIDC, mTLS, RBAC)', 'oidc', 'mtls', 'rbac', 'authoris'], ['Data protection (classification, encryption, masking)', 'encrypt', 'mask', 'classif'], ['Trust boundaries/threats named', 'trust boundar', 'threat', 'stride']],
      model: 'OIDC for humans, mTLS + scoped tokens between services, RBAC/ABAC per resource; data classified with encryption and masking; trust boundaries drawn and top STRIDE threats mitigated.' },
    failures: { dims: ['architecture'], kp: [['Per-dependency failure behaviour', 'dependency', 'down', 'slow'], ['Resilience patterns composed', 'timeout', 'breaker', 'bulkhead'], ['Degraded modes designed', 'degrad', 'fallback', 'queue']],
      model: 'For each dependency: timeout, retry-with-backoff where idempotent, circuit breaker with a designed fallback, bulkheads; degraded modes pre-agreed with the business rather than improvised.' },
    audit: { dims: ['domain', 'security'], kp: [['Audit events as business facts', 'audit', 'who', 'evidence'], ['Immutability and retention', 'immutable', 'retention', 'append'], ['Reconstruction capability', 'reconstruct', 'prove']],
      model: 'Every significant action emits an audit event (actor, authority, data versions) to an append-only store with multi-year retention; "prove why X happened" is a rehearsed query.' },
    operations: { dims: ['architecture'], kp: [['SLIs/SLOs per journey', 'slo', 'sli', 'p99'], ['Correlation IDs and tracing', 'correlation', 'tracing'], ['Runbooks/alerts actionable', 'runbook', 'alert'], ['Deployment approach', 'canary', 'rolling', 'deploy']],
      model: 'SLIs per critical journey with SLO-based alerting, correlation IDs end-to-end, tracing, runbooks linked from alerts, and canary/rolling deployments with objective rollback triggers.' },
    cost: { dims: ['tradeoffs'], kp: [['Main cost drivers named', 'cost driver', 'storage', 'egress', 'licence'], ['Unit economics framing', 'unit', 'per transaction'], ['One concrete optimisation', 'rightsiz', 'schedule', 'tier']],
      model: 'Name the dominant cost drivers (compute headroom, storage growth, egress, licences), frame as unit cost per transaction, and offer one concrete optimisation with its trade-off.' },
    tradeoffs: { dims: ['tradeoffs'], kp: [['Two-three major trade-offs explicit', 'trade-off', 'chose', 'accepted'], ['Rejected alternatives named', 'rejected', 'instead of', 'alternative'], ['Conditions that would change the decision', 'would change', 'revisit', 'if']],
      model: 'State the two or three decisive trade-offs (what was gained, what sacrificed), the alternatives rejected and why, and the conditions under which you would revisit.' },
    summary: { dims: ['communication'], kp: [['Problem and shape in two sentences', 'problem', 'architecture'], ['Key decisions with trade-offs', 'decision', 'trade-off'], ['Top risk and next step', 'risk', 'next', 'validate']],
      model: 'Five sentences: the problem and drivers; the architectural shape; two decisive trade-offs; the top risk and the spike that retires it. Then stop talking.' }
  };

  function ex(id, title, banking, brief, overrides) {
    var steps = {};
    Object.keys(BASE).forEach(function (k) {
      var o = overrides[k];
      steps[k] = o ? { dims: o.dims || BASE[k].dims, kp: o.kp || BASE[k].kp, model: o.model } : BASE[k];
    });
    return { id: id, title: title, banking: banking, brief: brief, steps: steps };
  }

  D.sysdesign = [
    ex('sd1', 'Customer Onboarding', true,
      `Design digital customer onboarding for a retail bank: application through verification, KYC, screening, approval and account opening. Target ≤ 1 day with controls intact. (Case study cs10 is the model-solution companion.)`,
      { entities: { model: 'Application (state machine: SUBMITTED→VERIFYING→SCREENING→PENDING_APPROVAL→PROVISIONING→ACTIVE), Applicant (pre-customer PII with its own retention rules), VerificationResult, RiskRating (model-versioned), Approval (maker+checker). SoR: onboarding owns applications; the party master takes over at activation.' },
        hla: { model: 'Orchestrated workflow (human tasks, visibility, audit demand it): capture channel → orchestrator with explicit state → async vendor legs (identity verification, screening via the screening platform\'s contract) behind ACLs with queue pacing → risk-tiered approval (straight-through for low risk, four-eyes queues above thresholds) → core account opening → activation events.' },
        deepdive: { model: 'The orchestrator: persistent state machine, idempotent stage transitions, savepoints for applicant resume, per-stage SLA timers with escalation, compensation on late rejection (cancel pre-provisioned account, audited). Vendor legs bounded by timeouts + breakers + durable queues.' },
        audit: { model: 'Every stage transition audited with input data versions; screening evidence follows the b5 model (list version, decision, authority); provisional-activation decisions carry the policy version that allowed them; abandoned applications expire with retention-compliant purge — also audited.' },
        failures: { model: 'Verification vendor outage → queue + "in review" status + signed extended-outage policy; screening timeout → block-or-provisional per compliance-signed segment policy; duplicate submission → business-key dedupe (person+product+day) attaches to the existing application.' },
        tradeoffs: { model: 'Orchestration over choreography (visibility, audit, compensation) accepting a coordinator to keep thin; risk-tiered straight-through processing trading human review for speed on governed low-risk segments; provisional activation trading control timing for conversion, bounded by limits and made explicit as policy.' } }),
    ex('sd2', 'Payment Processing', true,
      `Design payment processing for SEPA batch + instant rails: 5M/day, 400/s peak, 10-second confirm-or-reject on instant, zero duplicate posting. (Companion: cs9.)`,
      { scale: { model: '5M/day ≈ 58/s average; salary-day peak factor ~5–7× → design point 400/s with 3× headroom, load-tested at month-end profile. Payment store ~1 TB/yr → date partitioning + queryable archive.' },
        entities: { model: 'Payment (state machine INITIATED→…→POSTED with TIMEOUT→INQUIRING resolution), Reservation (the anti-double-spend control), LedgerEntry (append-only), ReconciliationBreak. Payment service masters payment state; core masters balances.' },
        hla: { model: 'Shared componentised checks (validation, fraud scoring, sanctions with per-rail latency budgets) feeding two rail-appropriate spines: instant (event-driven paymentId-ordered state machine, scheme adapter managing the 10 s deadline, shadow-ledger posting) and batch (file intake, cut-off-aware scheduling). Five-layer duplicate defence: API idempotency keys, business dedupe window, inbox consumers, scheme checks, daily reconciliation.' },
        deepdive: { model: 'The scheme adapter: submission with deadline management, timeout → status inquiry → definitive state (customers never see "maybe"), idempotent resubmission only after proven-unsent, per-scheme rate and cut-off awareness.' },
        events: { model: 'payment.events.v1 keyed by paymentId — state-machine ordering is sacred: no retry topics on this stream (block-and-alert instead); outbox everywhere; separate consumer groups for notification, fraud feedback, audit, analytics.' },
        failures: { model: 'Scheme timeout → INQUIRING → resolve via status API; core batch window → shadow-balance authorisation under signed limit policy, queue-and-post at reopen, window-close reconciliation; orchestrator DB loss → stop intake, restore, reconstruct from outbox + scheme records, reconcile before resume.' },
        tradeoffs: { model: 'Dual spines over one engine (rail physics differ) at the cost of duplicated plumbing; RPO-0 scoped to the payment store only (sync-standby latency accepted there, not estate-wide); pre-computed sanctions status trading a bounded, compliance-signed staleness window for the 10 s budget.' } }),
    ex('sd3', 'Partner & Business Relationships', true,
      `Design the relationship domain: typed, dated party-to-party relationships, roles and UBO traversal feeding screening scope. (Companion: cs15.)`,
      { entities: { model: 'Relationship (subject, object, type, share, validity, evidence, assertion source), RoleAssignment, UboComputation (inputs, rule version, result — reproducible). Relational SoR with constraints; a derived graph view for investigator exploration, rebuildable by definition.' },
        events: { model: 'RelationshipCreated/Expired/PartnerRoleChanged keyed by subjectPartyId (per-party scope-recompute ordering); BeneficialOwnerUpdated with SET semantics per uboSetVersion (whole-set replacement = idempotent consumers, no delta-ordering bugs). Restricted topic — ownership data is confidential.' },
        deepdive: { model: 'UBO traversal: versioned jurisdiction rule sets, effective-share computation through chains, cycle detection (A→B→C→A happens via funds) terminating with a flagged partial result that fails toward EDD, full computation evidence retained.' },
        failures: { model: 'Conflicting assertions → steward queue with both retained and provenance; missed relationship event downstream → bidirectional reconciliation of active relationships vs screening scopes; upstream party merge → re-key events handled, verified by post-merge reconciliation.' },
        audit: { model: 'Assertion provenance (who claimed, on what document) is evidence; descoping decisions audited ("why did screening stop for X"); UBO computations reproducible with rule versions — a regulator question with a query answer.' },
        tradeoffs: { model: 'Dedicated domain over relationship-tables-in-party-master (different cadence, consumers, sensitivity); relational SoR + derived graph over graph-as-SoR (audit/constraint posture wins the SoR role); set-semantics UBO events over deltas (idempotency over payload economy).' } }),
    ex('sd4', 'Customer Screening', true,
      `Design customer and partner screening: three triggers (onboarding, data changes, list updates), vendor matching, cases with four-eyes, 10-year evidence. The flagship. (Companion: cs11.)`,
      { scale: { model: '3M parties, 40k changes/day (≈0.5/s — trivial interactively); the sizing exercise is the list-delta re-screen: ~1M matches at a bulkheaded 80 req/s of the vendor\'s 100 ≈ 3.5 h — fits overnight with alerting margin; 20 req/s reserved keeps interactive p99 ≤ 5 s.' },
        hla: { model: 'Party/relationship events (inbox-deduped) → orchestration (scope computation incl. UBO, outbox-persisted requests, business-key idempotency partyId+trigger+listVersion) → vendor adapter (ACL, timeout/breaker/retry-topics, durable queue on outage) → results → case management (one case per hit via constraint, four-eyes state machine, ES read model) → evidence store (append-only, sync-replicated).' },
        deepdive: { model: 'The case-integrity pair: hit-without-case (orphan hits page compliance — the dangerous direction) and duplicate-cases (constraint + inbox catch; merges audited) — enforced by uniqueness on hitId plus bidirectional hit↔case reconciliation.' },
        events: { model: 'screening.requests/results/hits.v1 per the event catalogue: requests keyed partyId with retry-topics (reorder tolerable — downstream version checks), hits restricted-ACL keyed hitId (the idempotency anchor), DLTs that page (a silent drop is a control failure).' },
        audit: { model: 'Evidence per decision: party data version, list version, matching-logic version, raw matches, analyst view, decision, maker+checker — append-only, RPO-0 (scoped sync replication), 10-year retention with rehearsed retrieval ("all decisions under list v42").' },
        failures: { model: 'Vendor outage → breaker + durable queue + signed block-vs-provisional policy; missed event → count reconciliation + version full-sync + heartbeat canary; replay duplicates → business-key idempotency with dedupe retention ≥ replay horizon; list update fails midway → checkpointed restartable batch.' },
        tradeoffs: { model: 'Hybrid sourcing (vendor engine, owned orchestration/case/evidence) paying integration cost for evidence sovereignty; retry-topic ordering sacrificed on requests but never on case decisions; RPO-0 scoped to evidence only.' } }),
    ex('sd5', 'Transaction Monitoring', true,
      `Design AML transaction monitoring: 10M tx/day, rule+model scenarios in two latency tiers, alerts into case management, SAR path, full reproducibility. (Companion: cs12.)`,
      { scale: { model: '10M/day ≈ 120/s — streaming-comfortable; the binding constraint is human: ~10k raw alerts/day vs 2k analyst capacity → false-positive reduction is a hard requirement; 90-day-window scenarios are warehouse/batch-tier by physics.' },
        hla: { model: 'Transaction events enriched with customer/relationship context (event-fed local views) → two detection tiers (streaming for velocity/real-time patterns; nightly batch for long-window aggregations) sharing one versioned scenario registry → scored, deduped, party-correlated alerts → case management with SAR workflow behind tipping-off walls.' },
        deepdive: { model: 'The scenario registry: every rule/model versioned, approved, backtested before deploy; alerts carry scenario version + input snapshot hash; suppression rules versioned and four-eyes-governed (a suppression is a hole by design); per-scenario kill switches.' },
        failures: { model: 'Streaming state loss → rebuild from changelogs + replay with idempotent alert emission (scenario+entity+window keys); late transactions → watermark tolerance, beyond-window to batch catch-up; stale enrichment → context-version flags, high-risk scenarios pause on material staleness.' },
        audit: { model: 'Alert reproducibility is the audit design: inputs snapshot, logic version, score — "why did alert 999 fire" answered exactly; coverage matrix (typology → scenarios) maintained as regulator evidence; SAR confidentiality walls with their own access audit.' },
        tradeoffs: { model: 'Two runtime tiers over one (window physics); vendor scenario content licensed for typology credibility while owning the pipeline; false-positive reduction accepting governed recall risk — thresholds compliance-owned with backtests.' } }),
    ex('sd6', 'Fraud Detection', true,
      `Design real-time fraud scoring on the payment path: ≤150 ms budget, block/step-up/allow, model governance, fail-safe degradation. (Companion: cs13.)`,
      { scale: { model: 'Budget decomposition IS the scale answer: features ≤30 ms (precomputed aggregates in low-latency store), inference ≤50 ms, rules+decision ≤20 ms, transport margin — forbidding synchronous core-DB feature queries by arithmetic.' },
        hla: { model: 'Streaming feature platform (per-account/party/device aggregates, versioned, changelog-rebuildable) + stateless decision service (registry-loaded model versions, governed rules overlay, champion/challenger in shadow) + feedback loop (disputes/chargebacks label decisions; drift monitoring) + review-tier case handoff.' },
        deepdive: { model: 'The decision service: allow/step-up/review/block with coded reasons, every decision logging feature-vector hash + model + threshold versions (reproducibility for disputes and model governance), instant version rollback via the registry.' },
        failures: { model: 'Scorer degraded → conservative rules-only fallback + loud alerts + flagged decisions — fail-SAFE, never silent fail-open (the exploitable hole); stale features → context-age flags then rules-tier; bad model rollout → challenger gates + registry rollback; drift → freeze auto-actions above severity.' },
        security: { model: 'Coded reason codes outward (fraudsters probe logic); four-eyes on model/threshold changes; behavioural-PII feature store classified with access audit; adversarial testing (GPS/device spoofing) in the regime; step-up authentication as the friction-graded alternative to blocking.' },
        tradeoffs: { model: 'Precomputed features trading seconds of aggregate staleness for the latency budget; champion/challenger doubling inference cost for safe evolution; review-tier size as a staffed-capacity business decision, not an engineering constant.' } }),
    ex('sd7', 'Notification Platform', false,
      `Design the enterprise notification platform: email/SMS/push for 20+ producer teams, OTP-to-marketing priority tiers, consent enforcement, regulatory-notice evidence. (Companion: cs4.)`,
      { hla: { model: 'Producers publish NotificationRequested (versioned contract, business idempotency keys) — fire-and-forget so platform outages never block producers; the platform validates, enforces consent/preferences centrally, renders governed templates, routes via priority lanes (OTP drains first) to provider adapters with health-based failover, tracking per-notification state.' },
        events: { model: 'requests.v1 keyed by recipientId (per-recipient ordering enables digests/quiet-hours), lane topics per channel+tier with governed drain rates matching provider quotas, status.v1 for producers who care, DLTs per lane with alerting.' },
        deepdive: { model: 'Provider adapters: per-provider rate governance, breaker + failover (mandatory for OTP), delivery-state tracking (SENT vs DELIVERED distinction), vault-issued credentials — retiring the nine-account application.yml sprawl.' },
        audit: { model: 'Regulatory notices ride a dedicated lane with delivery-evidence capture (provider receipts, timestamps), escalation on failure, and 7-year evidence retention — "prove the notice reached the customer" is a designed query.' },
        failures: { model: 'Provider outage → breaker + secondary failover (OTP) or queue-and-drain (marketing); platform DB down → Kafka buffers ingest, producers unaffected (the design\'s central promise); template bug → per-template kill switch, DLT replay after fix.' },
        tradeoffs: { model: 'Fire-and-forget producer contract (isolation) trading direct producer control; central consent enforcement (one control point) trading team autonomy; per-recipient keying accepting negligible hot-recipient risk for ordering value.' } }),
    ex('sd8', 'Banking Audit Platform', true,
      `Design the central audit and evidence platform: every domain's audit events, immutable, complete, retrievable for a decade. (Companion: cs17.)`,
      { scale: { model: '50M events/day ≈ 600/s — Kafka-shaped; storage dominates: ~180 TB over 10 years → tiered (hot 90 d, warm 2 y, cold archive) with retrieval capability at EVERY tier — archived evidence you cannot retrieve is evidence you do not have.' },
        hla: { model: 'Published audit-event contract (actor, action, authority, entity refs, data versions, correlation) enforced by fitness checks; Kafka-mediated ingestion (producer isolation — audit emission via outbox shares the business change\'s fate); append-only sync-replicated store with hash-chained streams; indexed rebuildable retrieval views; litigation holds overriding retention automation.' },
        deepdive: { model: 'Completeness monitoring: expected-volume baselines per domain with absence alerting (a domain that stops emitting is the failure mode — nothing errors, gx27); periodic hash-chain integrity verification with the highest-severity incident path on failure.' },
        security: { model: 'Access to audit data is the insider crown jewel: strict entitlements, four-eyes on bulk export, every query itself audited (the auditors-audited loop), admin plane behind PAM; deletion where lawful via crypto-shredding, itself audited.' },
        failures: { model: 'Ingestion outage → Kafka buffers, producers unaffected; recovery runs completeness reconciliation proving no gaps; integrity-check failure → forensics scoped by chain anchors, regulator communication per counsel; a silent non-emitting domain → absence alerting.' },
        tradeoffs: { model: 'Central platform (completeness verifiability, one retention regime) trading a governed contract imposed on domains; RPO-0 justified here precisely because the store is small and append-only; Kafka-mediated emission over sync API (isolation over simplicity).' } }),
    ex('sd9', 'Customer Master Data', true,
      `Design the party/customer master: golden source consolidating three disagreeing legacy masters, feeding screening, payments, CRM. (Companion: cs14.)`,
      { scale: { model: '4.2M source records → ~3M golden; ~15% conflicting → ~600k conflicts, ~80% auto-resolvable by survivorship rules → ~120k steward decisions: the HUMAN pipeline is the critical path (20 stewards × 100/day ≈ 60 working days) — size it like a component.' },
        entities: { model: 'Party (golden, bitemporal — "what did we believe on date X" is a regulator query), SourceLink, MatchDecision (score, rule version, steward), SurvivorshipRule (versioned per attribute), MergeEvent (with old→new mappings). No silent auto-merge of distinct persons — a wrong merge is wrong-party screening.' },
        events: { model: 'customer.events.v1 keyed partyId — the estate\'s most-consumed topic, strictest schema governance; merge/unmerge events with re-key mappings as a PUBLISHED consumer obligation; legacy CDC feeds during migration, transformed at the boundary.' },
        deepdive: { model: 'The unmerge path, designed before go-live: split records, reassign source links, emit re-key events, downstream reconciliation verifies screening re-scoped and payment history reattributed — rehearsed, audited, because wrong merges happen.' },
        failures: { model: 'Wrong merge post-transactions → the rehearsed unmerge; garbage from a legacy feed → quarantine + steward triage, never auto-applied; consumer misses a re-key event → periodic full-sync reconciliation catches divergence.' },
        tradeoffs: { model: 'New golden source over conforming to a legacy master (consolidation pain once vs defects forever); bitemporal storage complexity justified by listed regulator queries; steward-gated medium-confidence merges trading throughput for the no-wrong-merge guarantee.' } }),
    ex('sd10', 'Regulatory Reporting', true,
      `Design the regulatory-reporting platform: 60 reports, point-in-time correct, lineage-traceable, deadline-bound, reproducible for years. (Companion: cs16.)`,
      { hla: { model: 'Versioned ingestion (immutable per-source snapshots; bitemporal store where "as known on date X" is demanded) → transforms versioned as git releases → report run = (input versions × logic version) recorded, making reruns byte-identical by construction → maker-checker sign-off gating submission → controlled resubmission with difference reports.' },
        deepdive: { model: 'The orchestration layer: dependency-graph scheduling back-calculated from deadlines with LATE-START alerting (the 06:00 submission makes a 02:00 stage delay a compliance clock, not an ops curiosity); rerun tooling with reserved quarter-end capacity.' },
        audit: { model: 'Lineage captured automatically from the pipeline (hand-maintained lineage is fiction by month three); reports + inputs + logic versions retained 10 y; a regulator query on one figure from two years ago traces to sources within the 24 h SLA — rehearsed.' },
        failures: { model: 'Source late → late-start ambers hours before deadline risk; wrong figure post-submission → resubmission-as-new-version with diff + root cause (never edits — the spreadsheet era\'s unlearning); source restates its data → downstream re-runs with new input versions, both retained.' },
        tradeoffs: { model: 'Immutable snapshot storage cost vs one failed audit (priced, cheap); selective bitemporality (only where "as known" queries exist); deliberate hotfix friction — a corrected report is a new version with evidence, slower and right.' } })
  ];
})(window.SAA.data);
