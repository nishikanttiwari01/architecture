/* labs.js — hands-on architecture labs.
   Compact: [id, title, difficulty, duration, scenario, input, instructions[], deliverables[], hints[], modelAnswer, rubric[], mistakes[], reflection[]] */
(function (D) {
  'use strict';
  var L = [
  ['lab1', 'Break a Monolith into Modules', 'Intermediate', 60,
   'RetailCo\'s order monolith (orders, customers, catalogue, pricing, shipping, invoicing, notifications) is one codebase where everything calls everything. The team wants module boundaries before considering services.',
   'Facts: pricing changes weekly (business-driven); catalogue is read-heavy, updated by a supplier feed; invoicing must be strongly consistent with orders; notifications fan out from everything; two teams (Commerce, Fulfilment) will own the code.',
   ['List candidate modules from the capability nouns', 'Assign each module\'s owned data (no shared tables)', 'Mark every cross-module call and classify: in-process interface or internal event', 'Check each boundary with the transaction test and change-cadence test', 'Map modules to the two teams'],
   ['Module list with owned data per module', 'Interaction table (caller → callee, sync/event)', 'Team ownership mapping', 'One boundary you deliberately did NOT draw, with reasoning'],
   ['Start from reasons-to-change, not nouns: pricing changes weekly — that volatility wants isolation.', 'Invoicing + orders needing strong consistency is a hint they share a transaction boundary — maybe one module.', 'Notifications consumed from everywhere = a natural internal event consumer.'],
   `A defensible cut: **ordering** (orders + invoicing together — the transaction test binds them), **catalogue** (read-heavy, feed-updated, owns product data), **pricing** (isolated for change cadence; called in-process by ordering with a defined interface), **customer**, **shipping**, **notification** (event consumer only). Commerce owns catalogue/pricing/customer; Fulfilment owns ordering/shipping; notification goes to whichever team is less loaded — it has the fewest domain rules. Cross-module: ordering→pricing (sync interface, needs answer now), ordering→shipping (event OrderReadyToShip), everything→notification (events). The boundary NOT drawn: orders vs invoicing — splitting them would force a saga where a local transaction suffices today.`,
   ['Modules mirror capabilities and change cadence', 'Each module owns its data exclusively', 'Sync vs event chosen per interaction with reasons', 'Transaction test applied (orders/invoicing)', 'Team mapping respects Conway'],
   ['Cutting by nouns into 8+ fragments', 'Leaving shared tables "for now"', 'Making every interaction an event (pricing needs an answer)'],
   ['Which module would you extract to a service first if ordered to, and why?', 'What fitness function would keep these boundaries honest?']],

  ['lab2', 'Decide Whether Microservices Are Justified', 'Intermediate', 45,
   'A 6-person team builds a new insurance-claims product. The CTO read that "modern systems are microservices" and asks you to assess.',
   'Facts: one team; domain partially understood (claims rules still evolving); expected load 50 req/s peak; no 24/7 ops capability yet; deadline 6 months; the company runs one shared Kubernetes cluster with a platform team.',
   ['List what microservices would buy THIS team, concretely', 'List the premium they would pay, concretely', 'Apply the d9 decision factors: team scale, domain clarity, ops maturity, scaling pressure', 'Write a one-paragraph recommendation with an explicit revisit trigger'],
   ['Benefit/cost table specific to this context', 'Recommendation paragraph', 'The ADR-style revisit condition'],
   ['Count the teams. Independent deployability for ONE team buys little.', 'An evolving domain means boundaries drawn now are guesses — cheap to move inside a monolith, expensive across services.', '50 req/s needs no partitioning heroics.'],
   `Recommendation: **modular monolith** with enforced boundaries (Spring Modulith/ArchUnit), one database with schema-per-module discipline, deployed on the existing cluster. Microservices would buy this team almost nothing (no team-parallelism problem, no divergent scaling) and cost real money: distributed failure handling, eventual consistency for claims workflows, N pipelines, and on-call surface a 6-person team cannot staff. Revisit triggers, recorded in the ADR: a second team joins the product; one module\'s load diverges 10×; or a compliance isolation demand appears. The module structure makes later extraction a refactoring, not a rewrite.`,
   ['Costs and benefits contextualised, not generic', 'Explicit recommendation, no fence-sitting', 'Revisit triggers concrete and measurable', 'Extraction path preserved via module discipline'],
   ['Recommending microservices to seem senior', 'Recommending a monolith with no boundary enforcement', 'No revisit condition — decisions rot'],
   ['What would change if there were 5 teams?', 'Which single module would you design most carefully for later extraction?']],

  ['lab3', 'Identify Bounded Contexts from Language', 'Intermediate', 45,
   'At a bank, you interview three departments about "the customer". Onboarding: "a customer is an applicant with documents and a risk rating — they might not have an account yet." Payments: "the customer is the account holder with mandates — I don\'t care about their documents." Compliance: "any party we screen is in scope — including directors who are not customers at all."',
   'The three quotes above, plus: onboarding hands over to account opening; compliance consumes data from both; a CRM also exists calling anyone with marketing consent a "customer".',
   ['Extract each department\'s model of "customer": attributes, lifecycle, identity', 'Mark where the same word means different things (context boundaries)', 'Name the bounded contexts and their core entities', 'Draw the context map: which relationships are upstream/downstream, where do ACLs belong?'],
   ['Per-context mini-glossary (term, meaning, lifecycle)', 'Named bounded contexts', 'Context map with relationship types'],
   ['Different lifecycles = different models: an applicant\'s life (draft→verified→approved) is not an account holder\'s.', 'Compliance\'s "party incl. non-customers" is the widest set — a hint it\'s a distinct party/screening context.', 'The CRM is a fourth context — conformist or ACL?'],
   `Contexts: **Onboarding** (Applicant: documents, verification state, risk rating; lifecycle ends at approval), **Account/Payments** (Account Holder: mandates, accounts; begins where onboarding ends), **Party/Screening** (Party: any screenable person/org incl. partners; supersets the others\' populations), **CRM** (Marketing Contact). Map: Onboarding → publishes ApplicantApproved consumed by Account (customer/supplier); Party/Screening consumes from both via published events with its own model (ACL at its boundary — it must not inherit onboarding\'s document-centric shape); CRM is downstream-conformist or ACL-protected, never upstream. The exercise\'s point: one enterprise "Customer" model would have to be all of these at once — which is why it always fails.`,
   ['Language differences drove the boundaries', 'Lifecycles distinguished per context', 'Party ⊃ customer relationship recognised', 'ACL placement justified'],
   ['Trying to unify into one Customer model', 'Boundaries by department org chart instead of language (they correlate here but the reasoning matters)'],
   ['Which context should master names/addresses, and why?', 'What breaks if CRM data flows upstream into screening?']],

  ['lab4', 'Define Service Boundaries for the Claims Domain', 'Advanced', 60,
   'The insurance product from lab 2 has grown: 4 teams now. Claims intake, assessment (rules + human adjusters), payout (money movement), fraud checking, document management, and customer communication all need homes.',
   'Facts: payouts must be idempotent and audited; assessment rules change weekly; fraud wants to consume everything; documents are large binaries; a claim\'s intake→assessment→payout flow is the core journey with human waits between steps.',
   ['Propose services with owned data', 'Apply transaction, chattiness, cadence and cognitive-load tests per boundary', 'Decide the claims flow coordination: orchestration or choreography, and why', 'Define the integration contracts (APIs vs events) per seam'],
   ['Service list with data ownership', 'Boundary-test results table', 'Coordination decision with rationale', 'Contract list per seam'],
   ['Human waits between steps = long-running workflow = orchestration territory.', 'Weekly-changing rules want isolation (and maybe a rules engine) inside assessment.', 'Money movement (payout) has the strictest controls — smallest, most protected service.'],
   `Services: **claim-intake** (claims, status), **assessment** (rules versions, decisions; adjudicator worklists), **payout** (payment instructions, idempotency keys, audit events — smallest surface, strictest controls), **fraud** (own risk store, consumes events from all), **document** (binaries + metadata, claim-checked references in events), **notification** (event consumer). Coordination: **orchestrated** claims workflow (explicit state, human tasks, compensations like payout-reversal; auditors will ask "where is claim X") — the orchestrator lives with intake or as its own thin service, commanding via events/commands and never absorbing assessment rules. Contracts: ClaimSubmitted/Assessed/PayoutCompleted events (fraud and notification subscribe); sync API only for assessment→document fetches during adjudication.`,
   ['Data ownership exclusive and stated', 'Boundary tests actually applied', 'Orchestration choice justified by workflow shape', 'Payout treated as the control-critical core'],
   ['A god "claims-service" owning the whole flow AND the domains', 'Choreographing a 5-step human workflow', 'Fraud reading other services\' tables "because it needs everything"'],
   ['Which boundary is most likely wrong, and how would you find out cheaply?', 'Where does the four-eyes control live for large payouts?']],

  ['lab5', 'Create a C4 System Context Diagram', 'Beginner', 30,
   'Draw (as structured text) the System Context for a customer screening platform.',
   'Users: compliance analysts, compliance managers, MLRO. Neighbours: party master (events in), relationship system (events in), screening vendor (REST out), core banking (holds out), audit store (events out), identity provider (authN).',
   ['Name the system with a one-line responsibility', 'List person-type users with their goals', 'List each neighbouring system with direction, protocol and purpose per interaction', 'Write the caption/legend line'],
   ['A structured-text context "diagram": system, actors, neighbours, labelled arrows'],
   ['Every arrow needs direction AND protocol AND purpose — "talks to" is a rumour.', 'The audit store is a neighbour, not a feature — that\'s a design statement.'],
   `**System:** Customer & Partner Screening Platform — screens parties against sanctions/PEP/media lists and manages resulting cases with full evidence. **Users:** Compliance Analyst (investigates cases), Compliance Manager (four-eyes approvals), MLRO (oversight, reporting). **Neighbours:** Party Master —(Kafka, Avro: party lifecycle events)→ Platform; Relationship System —(Kafka: relationship/UBO events)→ Platform; Platform —(REST/mTLS: match requests)→ Screening Vendor; Platform —(REST: place/release holds)→ Core Banking; Platform —(Kafka: audit events)→ Audit Store; Identity Provider —(OIDC: staff authN)→ Platform. Caption: C4 L1 — System Context, v1.0, owner: FinCrime Architecture.`,
   ['System responsibility in one line', 'All arrows labelled with direction+protocol+purpose', 'Users distinguished from systems', 'No internal containers leaked into the context level'],
   ['Drawing internal services at context level (zoom violation)', 'Unlabelled arrows'],
   ['Which neighbour would business stakeholders be most surprised by, and what conversation does that trigger?']],

  ['lab6', 'Create a C4 Container Diagram', 'Intermediate', 45,
   'Zoom into the screening platform from lab 5: define its containers.',
   'Known pieces from the b5 reference architecture: orchestration service, vendor adapter, case-management service, investigator UI, Oracle, Elasticsearch, Kafka topics, batch runner for list-update re-screening.',
   ['List each container: name, technology, one-line responsibility', 'Draw the interactions with protocols', 'Mark which container owns which data store', 'Sanity-check: does any container lack an owner or a reason to be separate?'],
   ['Container list with tech + responsibility', 'Interaction map', 'Data-ownership mapping'],
   ['The vendor adapter is separate from orchestration for a reason — name it (ACL + resilience isolation).', 'Elasticsearch is owned by case management as a rebuildable read model — who rebuilds it?'],
   `Containers: **screening-orchestration** (Spring Boot; consumes party/relationship events, decides scope, persists requests via outbox — owns Oracle screening schema), **vendor-adapter** (Spring Boot; ACL + Resilience4j around the vendor REST API; owns the retry queue), **case-management** (Spring Boot; cases/tasks/decisions, four-eyes; owns Oracle case schema), **investigator-ui** (SPA; talks to case-management via REST/OIDC), **search read model** (Elasticsearch; owned + rebuilt by case-management from case events), **batch-rescreen** (Spring Batch job runner; shares the orchestration domain, bulkheaded capacity), **Kafka** (screening.requests/results/hits.v1). Interactions labelled: orchestration →(outbox→Kafka) requests; adapter consumes requests →(REST/mTLS) vendor → publishes results; case-mgmt consumes hits, serves UI, indexes ES. Every store has exactly one owning container.`,
   ['Containers = deployable units with technology labels', 'Separation reasons articulable per container', 'Store ownership 1:1', 'Rebuild path for the read model stated'],
   ['Boxes for libraries or modules (wrong zoom)', 'Shared ownership of Oracle schemas'],
   ['Which container would you merge if the team were half the size?']],

  ['lab7', 'Create a Sequence Diagram with Failure Paths', 'Intermediate', 45,
   'Model "payment initiation" as a sequence — including what the happy-path version hides.',
   'Participants: client, payment-API, fraud-scorer, screening, core (reserve), outbox/Kafka. Constraints: p99 2s; screening may time out; core reservation may conflict.',
   ['Write the happy path as ordered steps', 'Add alt/timeout fragments: fraud-scorer slow, screening timeout, reservation conflict', 'For each failure: what does the client see, what state persists, what compensates?', 'Mark the idempotency and audit touchpoints'],
   ['Structured-text sequence with numbered steps and alt-fragments', 'Failure-outcome table (failure → client response → system state → recovery)'],
   ['A timeout is not a "no" — mark where outcome-unknown states appear and how they resolve.', 'The reservation is the step that must not double-apply.'],
   `Happy: 1 client→API POST /payments (Idempotency-Key) → 2 API stores key+request, 3 API→fraud score (150ms budget, timeout→conservative rules fallback), 4 API→screening check (500ms budget), 5 API→core reserve (optimistic on balance version), 6 API commits payment+outbox row, 7 relay→Kafka PaymentInitiated, 8 API→client 202 with status URL. Alt-fragments: **screening timeout** → payment persists in PENDING_SCREENING, client gets 202-pending, async completion or ops escalation at SLA (never silent skip); **reservation conflict** → retry use-case once, then 409 to client, nothing persisted beyond the idempotency record; **duplicate POST same key** → stored response replayed, no new flow; **fraud-scorer down** → rules fallback + degraded-mode audit flag. Audit events at steps 2, 5, 6; correlation ID from step 1 in every hop.`,
   ['Failure fragments present and specific', 'Outcome-unknown states resolved explicitly', 'Idempotency woven into the flow, not appended', 'Client-visible behaviour defined per branch'],
   ['Happy path only', 'Failures that "throw an error" with no state story'],
   ['Which branch would you load-test first?', 'Where would a retry storm originate in this flow?']],

  ['lab8', 'Write an ADR', 'Beginner', 40,
   'Your team must choose how the screening platform receives party changes: consume the party domain\'s Kafka events, or poll the party REST API every minute.',
   'Facts: party events exist (customer.events.v1, Avro, outbox-backed); polling is "simpler, we know REST"; screening must not miss changes (compliance); freshness target: 1 minute; team has basic Kafka experience.',
   ['Write the ADR: title, status, context, options (honest pros/cons), decision, consequences (incl. negative), review date', 'Make the compliance requirement do the deciding work', 'State the obligations the decision creates'],
   ['A complete short-form ADR'],
   ['Polling\'s real weakness here is the missed-change window and the load/freshness trade — quantify it.', 'The events option obligates you: inbox idempotency, lag monitoring, DLT. Consequences section!'],
   `**Title:** Consume party lifecycle events for screening triggers. **Status:** Proposed. **Context:** Screening must react to party changes within 1 minute and must not miss changes (compliance control). Party domain publishes outbox-backed Avro events; alternative is minute-polling the party API. **Options:** (A) Poll REST: +familiar, +no new infra dependency; −changes between polls invisible if API pagination/timestamps misused, −1-min worst-case staleness by design, −constant load, −no replay. (B) Consume events: +push freshness (seconds), +outbox guarantees publication, +replayable, +detective control possible (reconciliation vs party-change counts); −team must implement inbox idempotency, lag monitoring, DLT handling. **Decision:** Option B. The no-missed-changes requirement is the decider: events + reconciliation give a preventive AND detective control; polling gives neither cleanly. **Consequences:** (+) freshness, replay, audit-friendly trigger evidence; (−) Kafka operational learning curve — accepted; consumer must implement inbox dedupe (standard template); reconciliation job to be built (also required by compliance anyway). **Review:** 12 months, or if the party domain\'s event contract changes materially.`,
   ['Options genuinely argued, not strawmanned', 'Decision traced to the driver', 'Negative consequences owned', 'Review date present'],
   ['A one-option ADR', 'Consequences listing only positives'],
   ['Which single fact, if it changed, would flip this decision?']],

  ['lab9', 'Improve Weak Non-Functional Requirements', 'Beginner', 35,
   'Rewrite the five NFRs below from a real (anonymised) requirements document.',
   '1) "The platform must be performant." 2) "Downtime should be minimal." 3) "The system must handle future growth." 4) "All data must be secure." 5) "The system should be easy to audit."',
   ['Rewrite each using the six-part scenario form where useful', 'Add numbers with justifications (invent plausibly, note assumptions)', 'For each, name the design consequence your version creates'],
   ['Five measurable NFRs with stated assumptions', 'One design consequence per NFR'],
   ['The QAW drill has model shapes for all five patterns.', 'Numbers need sources: derive from business facts (users, cost of downtime) and label assumptions.'],
   `Model rewrites (yours may differ — the measures matter): 1) "At 500 req/s sustained (2× current peak), p95 < 400 ms, p99 < 1 s at the gateway" → drives capacity tests. 2) "99.9% monthly for the transactional journey (≤ 43 min); planned maintenance excluded via zero-downtime deploys" → drives multi-AZ + rolling releases. 3) "Sustains 3× current peak by horizontal scaling without redesign, verified by annual load test; 10× triggers the partitioning ADR" → drives statelessness now, defers sharding honestly. 4) "PII classified and masked in non-prod; mTLS everywhere; secrets vault-issued ≤24h; critical CVEs patched ≤72h; no PII in logs (CI-scanned)" → drives concrete controls. 5) "Every state-changing action reconstructible for 7 years: actor, authority, before/after versions, retrievable ≤24h" → drives audit-event contract + immutable store.`,
   ['Each rewrite measurable and testable', 'Assumptions labelled', 'Design consequences named', 'Percentiles used, not averages'],
   ['Replacing vague words with vague numbers ("very fast: 100ms")', 'Copying the model instead of deriving from context'],
   ['Which of the five would stakeholders push back on hardest, and what evidence would you bring?']],

  ['lab10', 'Select a Database', 'Intermediate', 40,
   'Choose stores for a fraud-detection platform\'s three data needs.',
   'A) Decision records: 10M/day writes, strict audit, queried by ID and by account+time. B) Feature aggregates: 50k reads/s at <5ms, updated by streaming jobs. C) Fraud-ring exploration: analysts traverse account→device→merchant links 3–6 hops deep, ad hoc.',
   ['For each need: identify the data shape, access pattern, consistency and volume', 'Choose a store family (not brand-first) with reasoning', 'Name the runner-up and why it lost', 'Define system-of-record vs derived roles'],
   ['Per-need: chosen family, reasoning, runner-up, SoR/derived role'],
   ['Need B\'s numbers (50k/s, <5ms) rule out disk-first stores.', 'Need C\'s question IS traversal — the one genuinely graph-shaped case.'],
   `A) **Relational, partitioned by date** (SoR): strict audit favours ACID + constraints; access by ID and account+time fits indexes; 10M/day is comfortable with partitioning. Runner-up: wide-column (scales further but sacrifices ad-hoc queries and constraint enforcement the audit posture wants). B) **In-memory key-value (Redis)** (derived): microsecond reads at 50k/s; rebuilt from streams — losing it degrades to recompute, acceptable. Runner-up: embedded/local caches (fastest, but consistency across scorer instances suffers). C) **Graph database** (derived from A + reference feeds): multi-hop ad-hoc traversal is the native workload; recursive SQL works at small depth but analyst ad-hoc 6-hop exploration will crawl. Runner-up honestly stated: recursive CTEs if volumes stay modest — revisit trigger recorded. All derived stores rebuildable from the relational SoR; that rebuild is tested.`,
   ['Family before brand', 'Numbers actually used in reasoning', 'SoR/derived discipline', 'Runners-up with real reasons'],
   ['One database for all three ("simplicity") ignoring the 5ms need', 'Graph for everything after need C'],
   ['What operational cost did adding two store families create, and is it justified?']],

  ['lab11', 'Design an Event-Driven Workflow', 'Advanced', 60,
   'Design the event flow for customer onboarding: application → verification → screening → approval → account opening.',
   'Constraints: humans approve above risk thresholds; screening may take minutes (vendor); every step audited; the applicant sees live status; steps may fail and resume.',
   ['Choose orchestration vs choreography and defend it', 'Define the events (names, producers, consumers, keys)', 'Design the state machine the applicant\'s status view reads from', 'Handle: screening timeout, approval rejection after account pre-provisioning, duplicate ApplicationSubmitted'],
   ['Coordination decision + event catalogue + state machine + three failure handlings'],
   ['Five steps, human tasks, audit visibility — the d2 heuristics all point one way.', 'The status view is an argument for explicit state (orchestrator) over event archaeology.'],
   `**Orchestrated** (workflow engine or dedicated onboarding-orchestrator): 5 steps, human waits, compensation, and "where is application 123?" all demand explicit state. Events (all keyed by applicationId on onboarding.events.v1): ApplicationSubmitted (channel → orchestrator; dedupe on business key applicant+product+day), VerificationCompleted, ScreeningRequested/Completed (bridged to the screening domain\'s own topics via its contract), ApprovalGranted/Rejected (human task outcome, maker≠checker), AccountOpened (account domain), OnboardingCompleted/Failed. State machine: SUBMITTED→VERIFYING→SCREENING→PENDING_APPROVAL→PROVISIONING→ACTIVE, with REJECTED/EXPIRED terminals — the UI reads orchestrator state, never infers from topics. Failures: screening timeout → state PENDING_SCREENING_RETRY, retry lane, ops alert at SLA, applicant sees "in review" (never the vendor\'s existence); rejection after pre-provisioning → compensation event AccountProvisioningCancelled (account domain closes/voids, audited); duplicate submission → orchestrator idempotent on business key, second submission attaches to the existing application and returns its status.`,
   ['Orchestration justified by the flow\'s shape', 'Events past-tense, keyed, owned', 'Status from state, not topic archaeology', 'All three failures resolved with state + compensation'],
   ['Choreographing then inventing a "status service" that re-derives state (an accidental orchestrator)', 'Exposing screening internals to the applicant'],
   ['Where does this design put the audit events?', 'What breaks if the orchestrator\'s DB is lost — and which lab-week topic answers it?']],

  ['lab12', 'Design Kafka Topics for the Order Domain', 'Advanced', 50,
   'An e-commerce order domain needs topics for: order lifecycle events (10k/s peak), inventory reservations (strict per-SKU ordering), and a full order-state changelog consumed by new read models.',
   'Consumers: fulfilment, notification, analytics (lifecycle); warehouse system (reservations); future read models (changelog). Peak 10k orders/s, avg order event 2KB, consumers can do ~10MB/s per partition.',
   ['Design topics: names, keys, partition counts (show the maths), retention/compaction', 'Justify topic-per-stream vs topic-per-event-type per case', 'Define ACLs/consumer groups', 'State the schema-governance setup'],
   ['Topic table: name, key, partitions, policy, rationale', 'Throughput calculation', 'Group topology'],
   ['Per-SKU ordering points the reservation key somewhere specific.', 'A changelog for rebuilding state has a cleanup.policy written all over it.', '10k/s × 2KB = 20MB/s → partitions from consumer throughput + headroom.'],
   `Topics: **orders.events.v1** — key orderId (lifecycle ordering per order), 20MB/s peak ÷ 10MB/s × 2 headroom = **4→ round to 6 partitions** (growth), retention 7d, one stream (fulfilment needs cross-event-type order per order). Groups: fulfilment, notification, analytics — three groups, independent. **inventory.reservations.v1** — key **skuId** (the strict ordering scope is the SKU, not the order!), partitions sized on SKU-event volume, retention 3d; single consumer group (warehouse) with instances ≤ partitions. **orders.state.v1** — key orderId, **compacted** (latest state per order = changelog for read-model bootstrap), tombstones on terminal cleanup. Schema registry, Avro, FULL compatibility on all three; topic ACLs per named group; DLTs per consumer with alerting. The deliberate lesson: two different keys (orderId vs skuId) because two different ordering requirements — one topic could not serve both.`,
   ['Keys derived from ordering scopes, maths shown', 'Compaction used where changelog semantics apply', 'Groups per logical consumer', 'Governance stated'],
   ['One key for everything', 'Partition counts guessed without arithmetic'],
   ['What happens to SKU ordering if reservations were keyed by orderId?']],

  ['lab13', 'Select Kafka Partition Keys', 'Intermediate', 35,
   'Choose keys for five topics and predict the failure of each plausible-but-wrong alternative.',
   '1) customer lifecycle events; 2) account postings; 3) payment state machine; 4) screening requests; 5) audit events (all domains, high volume, consumed only by the audit sink).',
   ['Choose each key with the ordering-scope argument', 'For each, name a tempting wrong key and its concrete failure', 'Flag any hot-key risks and mitigations'],
   ['Key table: topic → key → rationale → wrong-key failure'],
   ['Ask per topic: whose order does a consumer need?', 'Audit events are the trick entry — does ANY consumer need ordering?'],
   `1) **partyId** — customer\'s lifecycle stays ordered. Wrong: eventType → a customer\'s create/update interleave across partitions, stale overwrites. 2) **accountId** — balance derivation needs per-account sequence. Wrong: transactionId → perfect distribution, zero useful ordering. 3) **paymentId** — the state machine\'s own order. Wrong: accountId → one busy account serialises unrelated payments\' states behind each other (latency), and cross-payment ordering isn\'t needed anyway. 4) **partyId** — supersession logic per party. Hot-key risk: mega-corporates → capacity-plan or sub-key by party+listType if measured hot. 5) Trick answer: audit sink needs completeness, not order → **key by source domain or even null (round-robin)** for even spread; per-entity ordering reconstructs from timestamps+sequence at query time. Wrong: partyId "for consistency" → hot partitions for busy parties with no consumer benefiting.`,
   ['Ordering-scope reasoning per key', 'Wrong-key failures concrete', 'The audit trick spotted', 'Hot keys addressed'],
   ['Keying everything by the same entity out of habit', 'Random keys where ordering mattered'],
   ['Which of the five would you revisit first if partition counts had to grow?']],

  ['lab14', 'Handle Duplicate Events', 'Advanced', 45,
   'The case-management consumer of screening.hits.v1 processed hit H-42 twice last night, creating cases C-101 and C-102. Design the fix and the repair.',
   'Stack: Spring Boot consumer, Oracle, manual offset commit after processing. The duplicate came from a rebalance during a deploy. Cases already have analyst activity on C-101.',
   ['Diagnose why commit-after-processing still duplicated', 'Design prevention: inbox vs unique-constraint vs both', 'Design the repair for C-102 preserving evidence', 'Define the dedupe window vs replay policy'],
   ['Root-cause explanation', 'Prevention design with transaction boundaries', 'Repair runbook steps', 'Window/retention decision'],
   ['Processed-then-crashed-before-commit is at-least-once working as designed.', 'The unique constraint on hitId is the belt; the inbox is the braces — where does each live transactionally?'],
   `Root cause: processing committed to Oracle, the rebalance hit before the offset commit, the partition\'s new owner reprocessed — at-least-once behaving exactly as documented; the consumer was the naive party. Prevention: **unique constraint on cases.hit_id** (the business-key backstop that makes duplication structurally impossible) plus **inbox table** (message-id insert in the SAME transaction as case creation) for general dedupe of non-creating operations; graceful shutdown + cooperative rebalancing to shrink the window. Repair: merge C-102 into C-101 — reassign any activity, close C-102 with reason DUPLICATE_OF C-101, audit the merge (who, when, why), verify no downstream tasks/notifications spawned from C-102 (reconciliation query). Window: inbox retention ≥ max replay horizon (their replay policy allows 7 days → retain 14); the unique constraint has no window — that is why it is the backstop.`,
   ['At-least-once mechanics correctly explained', 'Constraint + inbox with correct transactional placement', 'Repair preserves evidence, audited', 'Window tied to replay policy'],
   ['Fixing by deleting C-102', 'In-memory dedupe proposals', 'Blaming Kafka'],
   ['Why does the unique constraint alone not cover update-type events?']],

  ['lab15', 'Handle Out-of-Order Events', 'Advanced', 45,
   'Screening consumed AddressChanged v7 then v6 (retry-topic overtaking). The party\'s screened address is now the older one, and a re-screen ran against it.',
   'Events carry entityVersion. The consumer currently applies whatever arrives ("last write wins" by arrival).',
   ['Design the consumer-side ordering defence', 'Decide: fix data forward or re-screen — and the audit consequences', 'Identify which topic-design choice caused overtaking and the alternatives', 'Write the detection that would have caught it'],
   ['Consumer defence design', 'Repair decision with evidence handling', 'Topic-design trade-off note', 'Detection/monitoring addition'],
   ['Version-checked upserts turn arrival-order into irrelevance.', 'A screen against stale data is an evidence problem, not just a data bug.'],
   `Defence: **version-checked apply** — update party snapshot only if incoming entityVersion > stored version; stale arrivals are acked, logged and counted (a metric, not an error). Repair: re-apply v7 (version check now passes), then **scoped re-screen** against the current address — the prior screen isn\'t deleted; the evidence chain shows screen-against-v6, correction, screen-against-v7, because a regulator asking "what did you screen on the 14th?" gets the truth. Cause: the retry topic sacrificed per-key ordering (k4\'s documented trade) — alternatives were block-and-alert on this stream or accepting the defence-in-consumer (chosen; retry throughput matters here and versions make reorder harmless). Detection: stale-version counter with alerting on rate; plus the periodic latest-screened-version vs current-party-version reconciliation that catches any residue.`,
   ['Version defence transactionally sound', 'Evidence-aware repair (no history rewriting)', 'Retry-topic trade-off articulated', 'Detection added, not just fix'],
   ['Sorting by timestamps across producers', 'Deleting the stale screen from history'],
   ['When would block-and-alert have been the better topic design?']],

  ['lab16', 'Handle Missed Events', 'Advanced', 50,
   'Audit finds a party whose address changed in March but was never re-screened. The event exists in Kafka (expired from retention by now); the screening consumer has no record of it.',
   'Suspects: a deploy that day with a consumer bug that acked without processing; DLT was empty; lag graphs show nothing (the message WAS consumed).',
   ['Explain how "consumed but not processed" evades lag and DLT monitoring', 'Design the detective control that catches this class', 'Design the repair for the affected population', 'Propose the consumer-side change preventing recurrence'],
   ['Failure-mode explanation', 'Reconciliation design (datasets, frequency, break workflow)', 'Repair/backfill plan', 'Prevention change'],
   ['Lag measures offsets, not effects — the gap between "read" and "did".', 'The reconciliation compares two systems\' counts of the same business fact.'],
   `Evasion: the consumer committed offsets after a code path that swallowed the event (a catch-and-continue bug) — lag stays zero, DLT stays empty, because both measure the transport, not the effect. Detective control: **windowed reconciliation of business facts** — party-change count (from the party domain\'s DB/audit) vs screening-requests-created count per window, with per-party drill-down on mismatch; daily, breaks worked within one business day; plus the periodic full comparison of latest-screened-version vs current-version per party (catches everything, eventually, regardless of cause). Repair: identify the affected window from deploy timestamps, query the party domain for changes in-window (source of truth outlives Kafka retention — by design), diff against screening requests, and batch-re-screen the gap with audit entries marking the reason. Prevention: the consumer\'s effect (request insert) and offset semantics tied via inbox pattern; error paths never ack-and-swallow — they retry or DLT; and a canary "heartbeat party change" flows end-to-end hourly, alerting on non-arrival.`,
   ['Transport-vs-effect distinction articulated', 'Reconciliation on business facts, not offsets', 'Repair uses the durable source, not expired Kafka', 'Heartbeat/canary included'],
   ['Adding more lag alerting (measures the wrong thing)', 'Assuming DLT emptiness means health'],
   ['Why must the repair source be the party DB rather than Kafka?']],

  ['lab17', 'Design an Idempotent API', 'Intermediate', 45,
   'Make POST /payments idempotent for a banking client that retries aggressively on any network hiccup.',
   'Requirements: same key → same result; same key + different body → explicit error; concurrent duplicates → one winner; keys expire eventually; the mechanism must survive service restarts and scale-out.',
   ['Design the key intake, storage and transactional placement', 'Define semantics: replay, conflict, in-flight concurrent, expiry', 'Specify response codes per case', 'State what the client contract documents'],
   ['Design (tables/flow)', 'Semantics table (case → behaviour → status code)', 'Client contract wording'],
   ['The idempotency record must commit WITH the payment — or a crash between them re-opens the hole.', 'Concurrent duplicates race on a unique constraint — the loser needs a defined experience.'],
   `Design: table idempotency_keys(key PK, request_hash, status[IN_FLIGHT|DONE], response_snapshot, created_at) in the payment DB. Flow: insert key+hash+IN_FLIGHT **in the same transaction opening the payment**; on completion update to DONE with the response. Semantics: replay with same key+hash → return stored response (200/201 as original); same key, different hash → 422 IDEMPOTENCY_KEY_REUSE; concurrent duplicate → unique-constraint loser polls/returns 409 RETRY_AFTER while winner is IN_FLIGHT, stored response once DONE; crash mid-flight → key row exists IN_FLIGHT with the (also uncommitted-rolled-back) payment absent → arriving retry finds no committed row (rollback removed it) and proceeds cleanly — this is why same-transaction placement is the crux. Expiry: TTL ≥ client max-retry horizon (contract says 24h), purge job partition-drops older rows. Contract wording: "Supply a unique Idempotency-Key per logical payment. Retries with the same key are safe and return the original result. Reusing a key with a different body is rejected. Keys are honoured for 24 hours."`,
   ['Same-transaction placement understood as the crux', 'All five semantic cases defined', 'Status codes sensible', 'Contract client-readable'],
   ['Redis SETNX detached from the payment transaction', 'No defined behaviour for the concurrent loser'],
   ['How does this interact with the outbox pattern in the same transaction?']],

  ['lab18', 'Design Retry and Dead-Letter Handling', 'Intermediate', 40,
   'The vendor-adapter consumer (screening requests → vendor REST) fails in three ways: vendor 429s, vendor 500s lasting minutes, and occasional malformed requests from a buggy upstream.',
   'Volume 50/s; vendor quota 100/s; ordering per party useful but not sacred (version checks exist downstream); compliance requires no silent drops.',
   ['Map each failure type to a handling tier', 'Design the retry-topic topology and the DLT lifecycle', 'Define alerting and the replay runbook', 'State the ordering consequence and why it is acceptable here'],
   ['Failure→tier mapping', 'Topology (topics, delays)', 'DLT lifecycle (alert→triage→fix→replay→evidence)', 'Ordering note'],
   ['429 is not an error to retry hot — it\'s the vendor telling you your own quota.', 'Malformed requests will never succeed — no retry tier fixes a poison message.'],
   `Mapping: **429** → in-consumer backoff honouring Retry-After, rate-limiter tightened (this is flow control, not failure); **500s minutes-long** → retry topics screening.requests.retry.5m → .30m with attempt-count headers, breaker on the vendor client meanwhile queueing naturally; **malformed** → straight to DLT (retrying a poison message is theatre). DLT lifecycle: publish with original payload+headers+error cause+offset; page the owning team (compliance: no silent drops — the alert is a control); triage runbook classifies producer-bug vs consumer-bug; fix upstream; replay tool re-emits to the main topic through the normal consumer (idempotent, so safe); closure notes which parties\' screening was delayed and for how long — evidence, because delayed screening is reportable in some regimes. Ordering: retry topics let later requests overtake retried ones — acceptable because downstream version checks make reorder harmless and throughput matters; documented in the topic catalogue as a conscious trade.`,
   ['Three failure types mapped differently', 'DLT has alert+owner+runbook+replay+evidence', 'Quota handling separated from failure handling', 'Ordering trade explicit'],
   ['One retry loop for everything', 'DLT as a write-only graveyard'],
   ['What changes if downstream had NO version checks?']],

  ['lab19', 'Perform Threat Modelling', 'Advanced', 60,
   'Threat-model the payment-initiation flow from lab 7.',
   'Elements: mobile client, API gateway, payment-API, fraud-scorer, screening, core, Kafka, Oracle. Data: PANs tokenised, balances, personal data. Staff admin plane exists for payment repair.',
   ['Draw the DFD in text with trust boundaries marked', 'Walk STRIDE per boundary-crossing element (min 8 threats)', 'Rate likelihood×impact and choose treatments', 'Derive three verification actions (tests/scans/pentest scope)'],
   ['DFD text with boundaries', 'Threat table (element, STRIDE, threat, rating, treatment)', 'Verification list'],
   ['Boundaries: internet→gateway, services→core, services→Kafka, humans→admin plane. Threats live at crossings.', 'The admin plane is the most privileged, least reviewed element — spend attention there.'],
   `Boundaries: internet|gateway, gateway|services, services|data(Oracle/Kafka), services|core, staff|admin-plane. Selected threats: **S** — stolen client token replayed against another API (mitigate: audience validation, short expiry); spoofed internal service (mitigate: mTLS identity). **T** — payment amount tampered between services (mitigate: mTLS integrity + signed critical fields); Kafka event tampered by compromised producer ACL (mitigate: per-principal ACLs, schema validation). **R** — operator denies a manual repair (mitigate: admin plane behind four-eyes + session recording + non-repudiable audit). **I** — balances leaked via verbose errors/logs (mitigate: error model, log allowlist); topic PII exposure (mitigate: ACLs, minimal payloads). **D** — payment-API flooded (mitigate: gateway rate limits, load shedding by priority). **E** — admin-plane privilege abuse (mitigate: time-bound PAM, least privilege, alerts on unusual repair volumes). Highest-rated: admin-plane R/E pair and token-audience S. Verification: pentest scoped to gateway+admin-plane; CI log-scanner for PII; chaos test proving rate-limit + shedding behaviour under flood.`,
   ['Boundaries drive the analysis', 'All six STRIDE letters represented plausibly', 'Admin plane treated as first-class attack surface', 'Verification tied to the model'],
   ['Listing generic threats unmoored from elements', 'Treating TLS as the answer to everything'],
   ['Which mitigation is a design change rather than a control bolt-on?']],

  ['lab20', 'Review a Flawed Architecture', 'Intermediate', 45,
   'A one-page proposal: "Orders are taken by order-service, which synchronously calls inventory, pricing, customer, loyalty and email services (all required for order placement). All services share the orders_db schema for speed. Deploys happen together on Fridays. If something fails, the client retries."',
   'The paragraph above is the entire input — as is depressingly common.',
   ['List every finding with severity', 'For the top three: risk narrative + concrete remediation', 'Write the three questions you would ask the team before judging further', 'Prioritise: what changes this week vs this quarter'],
   ['Findings list with severities', 'Top-3 deep-dives', 'Clarifying questions', 'Prioritised plan'],
   ['Count the availability of a 5-service synchronous chain.', '"The client retries" against non-idempotent order placement is a duplicate-order machine.'],
   `Findings: shared schema (Critical — coupling, corruption paths), 5-hop required-synchronous chain incl. email (Critical — availability multiplication; email should never gate order placement), client-retry without idempotency (Critical — duplicate orders guaranteed), Friday lockstep deploys (High — no independence, weekend incident risk), no timeout/fallback design mentioned (High), loyalty as required dependency (Medium — business question: really?). Top-3 remediations: idempotency keys on order placement (this week — cheapest, stops live duplicates); async email+loyalty via events (order placement needs neither synchronously); schema ownership split (quarter — the structural fix). Questions: what actually must be answered before accepting an order (drives the sync/async split)? what are current duplicate rates (evidence)? who owns each service (Conway check)? Priority: idempotency now, dependency-pruning next sprint, data ownership as the quarter\'s programme.`,
   ['Severity discrimination sensible', 'Remediation concrete and sequenced by cost/risk', 'Business questions asked, not just technical', 'Quick wins separated from structural work'],
   ['Everything labelled critical', 'Leading with the quarter-long fix while duplicates burn today'],
   ['Which finding would the business feel first, and does your priority reflect that?']],

  ['lab21', 'Identify Single Points of Failure', 'Intermediate', 40,
   'Audit this stack for SPOFs: two app instances behind ONE nginx on ONE VM; one Oracle primary (nightly backups, no standby); Kafka with 3 brokers but topics at replication-factor 1; one shared NFS mount for file exchange; the single office VPN concentrator through which all admin access flows; certificates manually renewed by one engineer.',
   'The list above; assume a banking-adjacent availability expectation (99.9%).',
   ['Enumerate SPOFs incl. the non-obvious ones', 'Rate each: blast radius × likelihood', 'Propose remediation with rough effort', 'Identify the human/process SPOFs alongside technical ones'],
   ['SPOF table: item, blast radius, likelihood, fix, effort', 'The one you would fix first, argued'],
   ['RF-1 topics make 3 brokers irrelevant — the data lives once.', 'The cert-renewal engineer is a SPOF with a holiday schedule.'],
   `SPOFs: nginx VM (total outage; fix: LB pair or cloud LB — low effort); Oracle primary (RTO = restore time, RPO = up to 24h!; fix: standby with replication — medium; the backup-only posture fails 99.9% arithmetic on its own); **Kafka RF-1** (broker loss = data loss despite the "cluster"; fix: RF-3 + min.insync=2 — low effort, do immediately); NFS mount (file flows halt; fix: object storage with retries — medium); VPN concentrator (admin lockout during incidents — the worst possible timing; fix: redundant access path — medium); cert renewal human (predictable outage on a date; fix: automated renewal — low). First fix: **Kafka RF**, because it is minutes of config away and converts silent data-loss risk into none; Oracle standby is the biggest project but the backup-only RPO is the finding to escalate today. Process SPOFs called out: cert engineer, and implicitly whoever knows nginx.`,
   ['RF-1 spotted despite "3 brokers"', 'Human SPOFs included', 'Effort-aware sequencing', 'RPO implication of backup-only Oracle stated'],
   ['Only hardware SPOFs listed', 'Fix-everything-at-once plans'],
   ['Which SPOF would an attacker prefer, and does that change priorities?']],

  ['lab22', 'Design a Disaster-Recovery Plan', 'Advanced', 60,
   'Write the DR plan skeleton for the screening platform (b5 architecture): Oracle, Kafka, Elasticsearch, vendor dependency, batch + interactive lanes.',
   'Business input: interactive screening RTO 1h/RPO 5min; batch re-screening RTO 24h; evidence store RPO 0 (regulatory); the vendor has its own DR taking "up to 4 hours".',
   ['Define scenarios (zone, region, data corruption, vendor loss)', 'Design per-component recovery meeting the stated objectives', 'Handle the RPO-0 evidence requirement honestly', 'Write the test regime'],
   ['Scenario × component recovery matrix', 'Evidence-store design note', 'Game-day schedule with pass criteria'],
   ['RPO 0 means synchronous something — scope it to the smallest store that needs it.', 'The vendor\'s 4h DR must appear in YOUR degraded-mode plan.'],
   `Scenarios: zone loss (multi-AZ handles: Oracle standby same-region, Kafka RF-3 across zones, ES replicas — RTO minutes, automated); region loss (Oracle Data Guard async to DR region — honest RPO ~seconds-to-minutes documented; Kafka MirrorMaker with procedural failover incl. offset translation and consumer dedupe window; ES rebuilt from Oracle/events — it\'s a read model, rebuild time ≤ 4h fits batch RTO); data corruption (point-in-time recovery + event replay after corruption point; corruption replicates, so PITR not replicas is the answer); vendor loss (their problem, your degraded mode: durable request queue, business-agreed onboarding policy, drain on recovery — 4h queue depth capacity-planned). Evidence store RPO 0: **synchronous replication scoped to the audit/evidence store only** (small, append-only, cheap to sync) — not the whole platform; the interactive lane keeps async economics. Tests: quarterly game days rotating scenarios, measured RTO/RPO vs promise reported to the board; annual full region failover; monthly restore test of Oracle PITR. Pass = objectives met AND runbook followed without improvisation.`,
   ['Objectives drive per-component tiers', 'RPO-0 scoped narrowly and honestly', 'Corruption treated distinctly from loss', 'Vendor DR integrated into degraded mode', 'Testing with pass criteria'],
   ['One giant sync-everything design', 'ES treated as needing the same tier as Oracle'],
   ['What did scoping RPO-0 to the evidence store save, roughly?']],

  ['lab23', 'Reduce Infrastructure Cost', 'Intermediate', 45,
   'A platform\'s monthly cloud bill: compute €48k (avg utilisation 19%), storage €22k (no lifecycle policies, 60% snapshots older than a year), egress €9k (analytics exports to on-prem BI nightly), non-prod €21k (24/7), DR standby region €18k (never tested). Total €118k.',
   'The numbers above; the business asks for 30% reduction without availability loss.',
   ['Attack each line with a specific mechanism and a saving estimate', 'Order by effort-to-saving ratio', 'Flag the one line where cutting creates risk, and the honest alternative', 'Produce the 30% plan'],
   ['Line-by-line savings table with mechanisms', 'Sequenced plan reaching ≥30%', 'Risk flag note'],
   ['19% utilisation is ~2.5× oversizing headroom even keeping 50% target.', 'An untested DR standby is cost WITHOUT protection — the fix is testing or re-tiering, not just deletion.'],
   `Plan: compute — rightsize to ~50% target utilisation + schedule scale-downs: 48→~26k (−22k); commitments on the new baseline: −4k more. Storage — lifecycle policies + snapshot retention (90d unless flagged): 22→~12k (−10k). Egress — stop nightly full exports; CDC/delta feeds or move BI queries to the cloud warehouse: 9→~3k (−6k). Non-prod — schedules (nights/weekends off ≈ 65% of hours) + rightsizing: 21→~8k (−13k). DR — the risk line: do NOT silently delete; either test it (making the spend real protection) or formally re-tier with business sign-off to pilot-light (−10k) given the tested RTO tolerance. Total savings ≈ €55–65k (46–55%) — comfortably past 30% with the DR decision made properly rather than by accountant. Sequence: schedules and lifecycle policies (week 1, zero risk), rightsizing (weeks 2–4, canary per service), egress re-plumb (month 2), DR decision (governance, month 2). Guard: unit-cost per transaction tracked so savings don\'t silently degrade capacity headroom for month-end.`,
   ['Mechanisms specific per line', 'DR handled as risk decision, not deletion', 'Sequencing by effort/risk', 'Peak-capacity guard retained'],
   ['Across-the-board percentage cuts', 'Deleting DR to hit the target'],
   ['Which saving is most likely to quietly reverse within a year?']],

  ['lab24', 'Modernise a Legacy Java System', 'Advanced', 60,
   'A Java 8 / Spring 4 monolith (no tests worth the name, JSP UI, one 400-table Oracle schema) must gain: an API for a new mobile app, faster releases, and eventually service extraction. Budget: one team, 9 months alongside BAU.',
   'Constraints: cannot freeze BAU changes; the DB is shared with two batch systems; the team knows the domain well.',
   ['Sequence the first 9 months: what order, what NOT to do', 'Design the API-enablement step without a rewrite', 'Plan the seam-making (branch by abstraction, modules) inside BAU flow', 'Define the exit criteria for month 9'],
   ['9-month sequenced plan', 'API approach', 'Seam strategy', 'Measurable month-9 criteria'],
   ['Characterisation tests before refactoring — you cannot refactor what you cannot verify.', 'The mobile API can front the monolith (facade) long before anything is extracted.'],
   `Months 1–2: safety first — characterisation tests around the flows BAU touches; CI pipeline; Java/Spring upgrade spike (the 8→17 path is mostly mechanical with tests in place). Months 2–4: **API facade** — a thin REST layer (new Spring Boot module or sidecar app) calling into the monolith\'s service layer, giving mobile its API against the EXISTING logic; no rewrite; contract-tested. Months 3–6: modularise in place — package-by-domain restructuring behind branch-by-abstraction seams, ArchUnit rules ratcheting (new violations fail, existing grandfathered and burned down); schema ownership mapping begins (which tables belong to which module — the batch-system shared tables flagged as the hard boundary). Months 6–9: first extraction candidate prepared (likely notification or document — low inbound coupling), extracted only if the seam is clean; releases move from monthly to weekly via pipeline + tests. NOT doing: big-bang rewrite, freezing BAU, extracting anything before module boundaries hold, touching the batch-shared tables. Month-9 exit criteria: mobile API live on the facade; weekly releases; ArchUnit violation count down 70%; one extraction done or consciously deferred with reasons; upgrade to supported Java/Spring complete.`,
   ['Tests-before-refactoring discipline', 'Facade-first API (value early, no rewrite)', 'Ratcheting fitness functions', 'Batch-shared tables respected as the hard edge', 'Measurable exit criteria'],
   ['Starting with extraction', 'Upgrade-everything-first with no safety net'],
   ['Why is notification a better first extraction than the account module here?']],

  ['lab25', 'Perform a Build-versus-Buy Assessment', 'Intermediate', 50,
   'Assess sourcing for transaction-monitoring (AML behavioural detection): vendor suite, open-source rules engine + custom build, or extend the existing fraud platform.',
   'Facts: regulator expects explainable, validated detection; team has fraud-platform experience; vendor licence ≈ €400k/yr; compliance wants scenario coverage documented against typologies; 18-month horizon pressure from an audit finding.',
   ['Apply hard constraint filters first', 'Score the three options across the b5-relevant criteria (use the matrix mentally)', 'Price hidden costs per option', 'Recommend with conditions and an exit note'],
   ['Constraint filter results', 'Scored comparison with hidden costs', 'Recommendation with conditions'],
   ['Model governance burden is the silently decisive criterion — who validates detection quality per option?', 'The audit-finding clock is a constraint, not a preference.'],
   `Filters: the 18-month audit clock eliminates nothing yet but weights time-to-coverage heavily; explainability eliminates any black-box option component. Scores (condensed): **Vendor** — fastest defensible coverage (pre-validated typology scenarios regulators recognise), licence + integration (≈1.5× licence) + tuning team anyway; lock-in and per-transaction pricing risks — negotiate caps + export. **OSS+build** — maximal fit and ownership, but YOU own model governance, typology coverage evidence, and validation — a permanent compliance-engineering team the org doesn\'t have; slowest to auditable coverage. **Extend fraud platform** — real synergies (data feeds, case UI) but fraud detection ≠ AML typologies; risk of one platform serving two regulators\' regimes badly; medium effort, medium coverage credibility. Hidden costs: vendor — customisation debt + exit; build — governance staffing forever; extend — the entanglement of two regulatory regimes in one codebase. Recommendation: vendor for the detection engine (coverage + validation amortised across their client base) with the case-management/evidence layer on the existing platform behind an ACL; conditions — scripted PoC on your data before signature, exit/export clause, renewal cap, and scenario-coverage mapping owned by compliance not the vendor. Revisit at 3 years with actual tuning-cost data.`,
   ['Constraints applied before scores', 'Model-governance burden recognised as decisive', 'Hybrid recommendation with ACL boundary', 'Contract conditions named'],
   ['Pure licence-cost comparison', 'Build recommended for engineering interest'],
   ['What evidence would justify switching to build at the 3-year review?']],

  ['lab26', 'Design a Zero-Downtime Migration', 'Advanced', 55,
   'Move the customer-preferences service from its old shared-schema Oracle home to its own PostgreSQL database — with zero downtime and zero lost writes, while three other apps still read the old tables.',
   'Volume: 200 writes/s; the three readers can be migrated to an API within the quarter but not before the move; rollback must remain possible for two weeks.',
   ['Sequence the migration phases with the dual-write/dual-read decisions', 'Design the reconciliation proving convergence', 'Keep the three legacy readers working throughout', 'Define rollback per phase'],
   ['Phase plan (expand→migrate→contract shaped)', 'Reconciliation design', 'Legacy-reader strategy', 'Rollback conditions per phase'],
   ['Dual-write is dangerous — prefer single-writer + CDC unless you must; if you must, bound it and reconcile hard.', 'The readers can be fed by a synced view long before they migrate to the API.'],
   `Phases: (1) **Expand**: stand up PostgreSQL, backfill snapshot, then CDC (Debezium) streams Oracle→Postgres continuously; reconciliation (counts+checksums per key range, hourly) proves convergence for a week. (2) **Switch writer**: service writes to Postgres ONLY (single-writer discipline — no dual-write window); reverse CDC Postgres→Oracle keeps the legacy tables fresh for the three readers. Rollback here = flip the writer back (both directions of sync exist; the two-week window keeps reverse-CDC alive). (3) Readers migrate to the service API one by one within the quarter (their timeline honoured — the reverse-synced tables serve them meanwhile). (4) **Contract**: after readers migrate + two clean reconciliation weeks, stop reverse CDC, archive old tables (read-only grace period), drop later. Zero-downtime because each switch is a config flip with both stores warm; zero-loss because CDC + reconciliation bracket every phase. Rollback conditions: reconciliation break rate > 0 unresolved for 24h, or writer-switch error-rate delta > 1% — automatic revert, investigate, retry.`,
   ['Single-writer discipline instead of casual dual-write', 'Reverse sync serves un-migrated readers', 'Reconciliation brackets every phase', 'Rollback objective and time-bounded'],
   ['Dual-write "carefully"', 'Forcing the three readers to migrate first (not your call)'],
   ['Why is reverse CDC safer than dual-writing from the service?']],

  ['lab27', 'Design a Customer Screening Platform (capstone)', 'Expert', 90,
   'Full design: the b5 screening platform, from events to evidence. This is the flagship banking lab — treat it as an interview final round.',
   'Inputs: 3M parties; ~40k party changes/day; list updates weekly (occasionally 40k-entry deltas); onboarding needs interactive decisions ≤5s (p99); vendor quota 100 req/s; evidence retention 10 years; four-eyes on hit release.',
   ['Design the end-to-end architecture (containers, topics, stores)', 'Do the capacity maths: daily change volume, list-update re-screen sizing vs vendor quota', 'Design all three screening triggers and their lanes', 'Handle five failure scenarios of your choice from the b5 list', 'Define the evidence model and its retention design'],
   ['Architecture description', 'Capacity calculation', 'Trigger/lane design', 'Five failure handlings', 'Evidence model'],
   ['40k-delta × affected-party matching vs 100 req/s: how many hours is a worst-case re-screen? Does it fit overnight?', 'The interactive 5s budget must survive the batch lane\'s hunger — bulkheads.'],
   `Architecture: as lesson b5/lab 6 — orchestration (outbox on Oracle), vendor adapter (ACL, Resilience4j, own queue), case management (hitId-idempotent, four-eyes state machine, ES read model), batch lane (Spring Batch, checkpointed), audit sink. Capacity: 40k changes/day ≈ 0.5/s average — trivial interactively; worst-case list delta requiring say 1M party×list matches at 100 req/s = ~2.8h — fits overnight IF batch gets ~80 of the 100 req/s (bulkheaded split: 20 reserved interactive ≈ handles 5s-p99 onboarding at observed 2–3/s with headroom). Triggers/lanes: onboarding (interactive lane, 5s budget: pre-checks + vendor call 2s timeout + policy fallback to pending-hold), data-change (event lane, minutes SLA), list-update (batch lane, overnight window with deadline alerting). Failure picks: vendor outage (queue+policy), DB-vs-Kafka (outbox), replay duplicates (business-key partyId+trigger+listVersion), hit-without-case (constraint+reconciliation), missed event (count reconciliation + heartbeat). Evidence: decision record = {party snapshot version, list version, matching-logic version, request, raw matches, scores, analyst view hash, decision, maker+checker identities, timestamps} — append-only store, synchronous replication (RPO 0), 10y retention with tested retrieval; case history event-sourced.`,
   ['Capacity maths done and driving the lane split', 'All three triggers designed distinctly', 'Five failures handled with named mechanisms', 'Evidence model complete incl. versions', 'Four-eyes in the state machine, not the UI'],
   ['Ignoring the vendor quota in re-screen sizing', 'Interactive and batch sharing capacity unbulkheaded'],
   ['Which single design element would you defend hardest in front of a regulator?', 'What did the 5s budget force you to pre-compute?']],

  ['lab28', 'Design a Payment Platform (capstone)', 'Expert', 90,
   'Design the b4/cs9 payment platform: multi-rail (batch SEPA + instant), 5M payments/day, peak 400/s, 99.95% availability for submission.',
   'Constraints: instant rail 10s confirm-or-reject; EOD batch windows exist; duplicate prevention is board-visible after last year\'s incident; reconciliation with schemes daily.',
   ['Design the pipeline with rail-specific paths', 'Engineer duplicate prevention at every layer', 'Design the state machine incl. unknown-outcome resolution', 'Size for peak and define the degradation ladder', 'Define reconciliations and their break workflows'],
   ['Architecture + pipeline per rail', 'Duplicate-defence layer map', 'State machine with timeout states', 'Capacity + degradation plan', 'Reconciliation designs'],
   ['The rails share validation/fraud/screening components but not their spines.', 'Every "sent" without "confirmed" needs a designed resolution path per rail.'],
   `Shared front: intake APIs (idempotency keys), validation, fraud scoring, sanctions check — componentised with per-rail latency budgets. Rails: instant — event-driven spine, inline screening (pre-computed status + payment-data check), reservation, scheme adapter with 10s deadline management (timeout → scheme status inquiry → definitive state; customer never sees maybe); batch — file/bulk intake, cut-off-aware scheduling, the same dedupe and audit components. Duplicate defence: API idempotency keys (same-tx storage), business dedupe window (debtor+creditor+amount+ref), inbox on every consumer, scheme duplicate checks, and the daily scheme reconciliation as the net — five layers, each covering the others\' gaps. State machine: INITIATED→VALIDATED→CHECKED→RESERVED→SENT→{CONFIRMED→POSTED | REJECTED→RELEASED | TIMEOUT→INQUIRING→resolved}. Capacity: 400/s peak × 3 headroom on the hot path, load-tested at month-end profile; degradation ladder (business-signed): defer batch lanes → shed statement/query traffic → tighten instant limits → never compromise dedupe or screening. Reconciliations: internal (DB states vs events, hourly), scheme (our sent/confirmed vs their files, daily, break workflow with four-eyes repairs), nostro (for correspondent legs). Availability: multi-AZ active-active for the stateless path, Oracle with sync standby (RPO 0 on the payment store), Kafka RF3.`,
   ['Rails separated where semantics differ, shared where identical', 'Five-layer duplicate defence articulated', 'TIMEOUT→INQUIRING→resolved path present', 'Degradation ladder business-signed', 'Three reconciliation types'],
   ['One pipeline pretending both rails are alike', 'RPO>0 on the payment store unacknowledged'],
   ['Which layer of duplicate defence has caught real incidents in your experience or reasoning, and why keep the others?']],

  ['lab29', 'Present Architecture to an Executive Audience', 'Advanced', 45,
   'Prepare the executive presentation for the lab-27 screening platform: the board must approve €1.8M and 9 months.',
   'Audience: CFO (cost-sceptical), COO (operations), CRO (risk — your ally), CEO (impatient). You get 10 minutes plus questions.',
   ['Write the opening 30 seconds verbatim', 'Structure the 10 minutes (slide headlines only)', 'Prepare answers to the four predictable challenges: why so expensive, why not the vendor\'s full suite, what if we wait, when do we see value', 'Define your ask precisely'],
   ['30-second opener verbatim', 'Slide-headline outline', 'Four challenge answers (2–3 sentences each)', 'The explicit ask'],
   ['Lead with the risk they already fear, priced — the CRO is your co-narrator.', 'Never show a container diagram to this room.'],
   `Opener: "Last year's audit found we cannot prove every customer change is screened. Today that gap is real: roughly 40,000 customer changes a day flow through systems with no detective control. I'm asking for €1.8M over 9 months to close it permanently — and I'll show you the two numbers that matter: what it costs, and what not doing it costs." Slides: 1 The gap in one picture (control flow with the hole) · 2 What it enables going wrong (scenario, priced) · 3 The fix in business terms (every change screened in minutes, every decision provable for 10 years) · 4 Options considered (vendor-everything €2.6M+lock-in / do-minimum €0.6M leaves detection gap / recommended €1.8M) · 5 Plan: value at month 4 (onboarding), month 7 (ongoing screening), month 9 (evidence platform) · 6 Risks and the ask. Challenges: expensive → "€1.1M is the vendor and integration either way; our €0.7M builds the evidence layer we keep across vendors." vendor suite → "their case tool locks our evidence into their exit terms — the one thing regulators require us to keep." wait → "each quarter of delay is ~10k unscreened-change-days and the finding stays open into next audit." value when → "month 4, onboarding decisions in 5 seconds; the audit finding closes at month 7." Ask: "Approve €1.8M, the 9-month plan, and protected capacity from the three named teams — decision today keeps the vendor pricing we negotiated."`,
   ['Opener leads with priced risk, not technology', 'Options shown incl. cheaper ones honestly', 'Value milestones before the end date', 'Ask explicit and time-bound', 'Zero jargon throughout'],
   ['Architecture diagrams in the deck', 'Defensiveness on the cost challenge'],
   ['Which answer would you rehearse most, and what number does it hinge on?']]
  ];

  D.labs = L.map(function (l) {
    return { id: l[0], title: l[1], difficulty: l[2], duration: l[3], scenario: l[4], input: l[5],
      instructions: l[6], deliverables: l[7], hints: l[8], modelAnswer: l[9], rubric: l[10],
      mistakes: l[11], reflection: l[12] };
  });
})(window.SAA.data);
