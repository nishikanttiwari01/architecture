/* events.js — banking event catalogue + Kafka decision guides.
   Event format: [name, domain, producer, consumers, key, partition, payload, sensitive, ordering, idempotency, retry, audit, evolution, reconciliation] */
(function (D) {
  'use strict';
  var E = [
  ['CustomerCreated', 'Customer', 'Party/Customer service (owner of party master data)',
   'Screening, CRM, notification, audit, account-opening workflow',
   'partyId', 'Hash of partyId on customer.events.v1 — all lifecycle events for one party stay ordered',
   'partyId, customerNumber, type (person/organisation), status, KYC status, createdAt; demographics by reference or in restricted stream',
   'Names/addresses are PII — prefer ID+status in the broad topic, details via authorised API or a restricted customer.pii.v1',
   'Must precede all other events for the party (guaranteed by keying); consumers must handle Created arriving after a replayed Updated via version checks',
   'Consumers create-if-absent keyed on partyId; inbox dedupe for redelivery',
   'Standard consumer retries → retry topic → DLT with alerting; screening consumer must never silently drop',
   'Creation is an auditable business fact — audit sink consumes; evidence includes source channel and operator',
   'Additive fields only in v1; new required fields → customer.events.v2 with parallel run',
   'Daily count of created parties vs CustomerCreated events published (outbox monitoring plus reconciliation)'],
  ['CustomerUpdated', 'Customer', 'Party/Customer service',
   'Screening (re-screen trigger), CRM, search index, audit',
   'partyId', 'Same topic/key as CustomerCreated — cross-event-type ordering per party is the point of a domain stream',
   'partyId, changedFields list, entityVersion, updatedAt; new values by reference for sensitive fields',
   'changedFields alone leaks little; full old/new values are PII — restrict or reference',
   'Per-party order critical: an older update must never overwrite a newer one — entityVersion in payload, consumers ignore stale versions',
   'Version-checked upsert makes processing naturally idempotent',
   'Retry topics acceptable (version checks defend against the reordering they cause)',
   'Update + who/what/when to audit; screening uses it as re-screen trigger evidence',
   'changedFields enum grows additively; renaming fields is breaking',
   'Party-change count vs screening-request count per window — the missed-event detector of lesson b5'],
  ['AddressChanged', 'Customer', 'Party/Customer service',
   'Screening (address affects matching), statements/communication, fraud (velocity signal), audit',
   'partyId', 'customer.events.v1 with partyId key',
   'partyId, addressType, effectiveDate, entityVersion; address value by reference or restricted stream',
   'Addresses are PII and banking-secrecy relevant — reference pattern preferred',
   'Ordered per party; effectiveDate handles backdated corrections distinctly from event order',
   'Version-checked; re-screen triggered at most once per version (business-key idempotency)',
   'Standard tiered retries; DLT alerts because a missed address change is a screening gap',
   'Full audit incl. channel and verification method',
   'Additive; address-structure changes (ISO 20022 alignment) may force v2',
   'Screening scope reconciliation: latest screened version vs current version per party'],
  ['AccountOpened', 'Account', 'Account service (wrapping the core)',
   'Customer 360, statements, fraud baseline, regulatory reporting, audit',
   'accountId', 'account.events.v1 keyed by accountId; consumers needing per-customer views re-key downstream',
   'accountId, partyId(s), productCode, currency, openedAt, branch, mandateRef',
   'Account existence can fall under banking secrecy — topic ACLs matter more than payload trimming here',
   'Per-account ordering; the open event precedes all postings by keying',
   'Create-if-absent on accountId',
   'Standard; failed consumers must catch up before EOD processing',
   'Core is system of record; event mirrors its audit entry with core transaction reference',
   'productCode vocabulary from reference data — additive evolution',
   'Accounts in core vs AccountOpened events — daily'],
  ['AccountClosed', 'Account', 'Account service',
   'Screening (descope), statements (final), regulatory reporting, dormancy/retention scheduler, audit',
   'accountId', 'Same stream as AccountOpened for per-account lifecycle order',
   'accountId, closedAt, reason, finalBalanceZeroed flag',
   'Closure reason may be sensitive (fraud, sanctions) — use coded reasons, details via restricted channel',
   'Must follow all postings for the account — closure emitted only after final posting confirmed',
   'Status-set idempotency (closed is terminal)',
   'Standard; downstream descoping verified by reconciliation, not assumed',
   'Closure + retention clock start are audit facts',
   'Reason-code enum additive',
   'Closed accounts vs downstream descoped states (screening, statements) — the relationship-expiry lesson applies'],
  ['PaymentInitiated', 'Payment', 'Payment service (orchestrator)',
   'Fraud scoring, sanctions screening, audit, customer notification (pending)',
   'paymentId', 'payment.events.v1 keyed by paymentId — the payment\'s own lifecycle is the ordering scope; account-level views derive downstream',
   'paymentId, idempotencyKey, debtorAccountRef, creditorRef (masked), amount, currency, requestedExecution, channel, status=INITIATED',
   'Creditor names and references are sensitive; remittance text can contain anything — mask/limit in broad topics',
   'Strict per-payment state-machine order: Initiated → Authorised/Rejected → Posted; consumers validate transitions',
   'paymentId + status transition = natural idempotency; duplicate Initiated with same idempotencyKey must not create a second payment',
   'NO retry topics on the ordered payment stream — block-and-alert or sequence-check; reordering a payment state machine is worse than delay',
   'Every state transition audited with acting system/user and mandate reference',
   'ISO 20022 field alignment; amount/currency formats frozen — new info additive',
   'Payments in DB per status vs events per status; plus scheme-side reconciliation downstream'],
  ['PaymentAuthorised', 'Payment', 'Payment service',
   'Posting/ledger consumer, notification, fraud (feedback), audit',
   'paymentId', 'Same stream — state machine integrity',
   'paymentId, authorisedAt, checksPassed [fraudScoreRef, screeningRef, mandateRef], reservationRef',
   'Check references, not check payloads — scores and hit details stay in their domains',
   'After Initiated, before Posted — consumers enforce the transition table',
   'Transition idempotency: authorising an authorised payment is a no-op',
   'As PaymentInitiated — ordered stream discipline',
   'Authorisation evidence chain: which checks, which versions, who/what authorised',
   'checksPassed list grows additively',
   'Authorised-without-posting detection: payments stuck in AUTHORISED beyond SLA page operations'],
  ['PaymentRejected', 'Payment', 'Payment service',
   'Notification, fraud (label feedback), originating channel, audit',
   'paymentId', 'Same stream',
   'paymentId, rejectedAt, stage, reasonCode (coded — never free-text hit details)',
   'Rejection reasons can reveal screening/fraud logic — coded reasons only; tipping-off rules may restrict customer-facing detail further',
   'Terminal state after any pre-posting stage',
   'Terminal-state idempotency',
   'Standard; notification consumer dedupes to avoid double customer messages',
   'Rejection + reason + stage fully audited (regulator asks why payments failed)',
   'reasonCode enum additive with documentation',
   'Rejected count by reason trended; spikes reconciled against rule/list changes'],
  ['TransactionPosted', 'Ledger/Account', 'Core posting engine (via CDC or core adapter)',
   'Balances/ODS, statements, fraud, transaction monitoring, warehouse, audit',
   'accountId', 'transaction.events.v1 keyed by accountId — per-account posting order is what statement and balance consumers need',
   'transactionId, accountId, paymentRef?, amount, currency, bookingDate, valueDate, balanceAfter?, code',
   'Transaction narratives are sensitive; balanceAfter convenient but increases exposure — decide deliberately',
   'Per-account order essential (balances derive from sequence); global order irrelevant',
   'transactionId uniqueness; consumers upsert by it',
   'Retry topics NOT acceptable for balance-deriving consumers — sequence checks or blocking',
   'The core\'s own ledger is the audit truth; events reference core entries',
   'Field-frozen core contract — changes are major events with long dual-run',
   'The flagship reconciliation: core postings vs events consumed, daily, count+sum+sample'],
  ['ScreeningRequested', 'Screening', 'Screening orchestration service',
   'Vendor adapter(s), audit, operational dashboard',
   'partyId', 'screening.requests.v1 keyed by partyId — a party\'s screening history stays ordered; requestId inside payload',
   'requestId, partyId, trigger (onboarding|dataChange|listUpdate|manual), scope [sanctions, pep, adverseMedia], listVersion, requestedAt',
   'Party identifiers only — matching data flows to the vendor over the direct integration, not the broad topic',
   'Per-party order useful (later request supersedes); requestId disambiguates',
   'Business-key idempotency: partyId+trigger+listVersion — the replay-duplicate defence of lesson b5',
   'Retry topics fine (vendor adapter tolerates reorder); DLT with loud alerting — a lost request is a compliance gap',
   'Request creation audited with trigger evidence',
   'scope/trigger enums additive; listVersion format frozen',
   'Party-changes vs requests (missed events); requests vs results (stuck screening)'],
  ['ScreeningCompleted', 'Screening', 'Screening orchestration (after vendor response)',
   'Onboarding workflow, case management (if hits), audit, dashboard',
   'partyId', 'screening.results.v1 keyed by partyId',
   'requestId, partyId, outcome (clear|hits), hitCount, listVersion, completedAt, vendorRef',
   'No match details in the broad event — hit payloads go via ScreeningHitCreated to restricted consumers',
   'Pairs with its request; per-party order for supersession logic',
   'requestId-keyed idempotency',
   'Standard tiered retries; onboarding consumer must handle late results (timeout policy separately)',
   'Completion + listVersion is core evidence — immutably audited',
   'Outcome enum stable; extend via additive fields',
   'Requests without completion beyond SLA → operational alert + investigation'],
  ['ScreeningHitCreated', 'Screening', 'Screening orchestration',
   'Case management (restricted), compliance dashboard, audit',
   'hitId', 'screening.hits.v1 keyed by hitId; restricted ACLs — smallest possible consumer set',
   'hitId, requestId, partyId, listEntryRef, matchScore, listVersion, createdAt',
   'Highly sensitive: being a potential sanctions match is toxic data — tightest ACLs, no fan-out, short broad-retention',
   'Ordering per hit trivially satisfied; case creation cares about existence, not order',
   'hitId is THE idempotency anchor: exactly one case per hit (unique constraint downstream)',
   'Retries standard; duplicate-case defence lives in the consumer',
   'Hit creation, score, and list entry are evidence — retained per screening retention schedule',
   'Score semantics versioned with matching-logic version in payload',
   'Hits vs cases bidirectional reconciliation (orphan hits page compliance, duplicates merge audited)'],
  ['CaseCreated', 'Investigation', 'Case-management service',
   'Notification (analyst worklists), SLA monitor, audit, reporting',
   'caseId', 'case.events.v1 keyed by caseId',
   'caseId, hitId?, type (screening|fraud|monitoring), priority, slaDue, createdAt',
   'Case metadata modest; the case content stays in the case store',
   'Per-case lifecycle order for downstream state tracking',
   'caseId create-if-absent; creation itself idempotent on source key (hitId)',
   'Standard; SLA monitor must not miss creations — lag alerting',
   'Creation + assignment audited; SLA clocks are regulatory in some regimes',
   'type/priority enums additive',
   'Cases vs source hits/alerts; open-case ageing report as operational reconciliation'],
  ['InvestigationTaskCreated', 'Investigation', 'Case-management service',
   'Worklist/assignment, SLA monitor, audit',
   'caseId', 'Keyed by caseId so a case\'s tasks stay ordered with its lifecycle',
   'taskId, caseId, taskType, assigneeRole, dueAt',
   'Low sensitivity at metadata level',
   'Per-case order (task after its case)',
   'taskId uniqueness; one-task-per-hit-per-type rules enforced with constraints',
   'Standard tiered',
   'Task lifecycle audited for evidence of diligence',
   'taskType additive',
   'Tasks per case consistency check; the duplicate-task drill of lesson b5'],
  ['RelationshipCreated', 'Relationship', 'Relationship service',
   'Screening (scope recomputation), credit, reporting, audit',
   'relationshipId with subjectPartyId in payload', 'relationship.events.v1 keyed by subjectPartyId — scope recomputation consumes per party',
   'relationshipId, subjectPartyId, objectPartyId, type (owns|controls|represents|guarantees), share?, validFrom, entityVersion',
   'Ownership structures are confidential — restricted topic, minimal fan-out',
   'Per-subject-party order so scope recomputation sees a consistent sequence',
   'relationshipId + entityVersion',
   'Standard; screening consumer criticality = high, DLT paged',
   'Relationship facts are KYC evidence — fully audited',
   'type enum additive; share semantics frozen',
   'Active relationships vs screening scopes — the expiry-drill reconciliation both directions'],
  ['RelationshipExpired', 'Relationship', 'Relationship service',
   'Screening (descope recomputation), credit, audit',
   'subjectPartyId', 'Same stream and key as RelationshipCreated',
   'relationshipId, subjectPartyId, expiredAt, reason',
   'As RelationshipCreated',
   'Must follow its Created (same key guarantees it); consumers tolerate replay pairs via versions',
   'Terminal per relationshipId; scope recomputation idempotent per party version',
   'Standard; the "expired but still screened" failure is caught by full-sync reconciliation',
   'Descoping decisions audited (why screening stopped)',
   'reason additive',
   'Expired relationships vs active screening scopes — scheduled full comparison'],
  ['PartnerRoleChanged', 'Relationship', 'Relationship service',
   'Screening (role affects treatment), mandate checks, audit',
   'subjectPartyId', 'Same relationship stream',
   'relationshipId, subjectPartyId, oldRole, newRole, effectiveAt, entityVersion',
   'Role data confidential as above',
   'Per-party order with entityVersion stale-check',
   'Version-checked upsert',
   'Standard tiered',
   'Role transitions are control-relevant (a signatory change affects mandates) — audited',
   'Role enum additive',
   'Role state vs downstream treatments (screening intensity, mandate validity)'],
  ['BeneficialOwnerUpdated', 'Relationship', 'Relationship service (UBO traversal recomputation)',
   'Screening (EDD triggers), KYC review scheduler, regulatory reporting, audit',
   'organisationPartyId', 'relationship.events.v1 keyed by the organisation whose UBO set changed',
   'organisationPartyId, uboSetVersion, ubos [{partyId, effectiveShare, basis}], computedAt',
   'UBO data is among the most sensitive KYC data — restricted, minimal consumers',
   'Per-organisation order; uboSetVersion supersedes wholesale (set semantics, not deltas)',
   'uboSetVersion idempotency — consumers replace the whole set per version',
   'Standard; missed UBO updates are EDD gaps — reconciliation mandatory',
   'Computation inputs (chain, shares) retained as evidence of traversal correctness',
   'basis enum additive; set semantics frozen',
   'Recompute-vs-stored spot checks; UBO sets vs screening scope of those UBOs'],
  ['FraudAlertCreated', 'Fraud', 'Fraud-detection engine',
   'Case management, payment orchestrator (hold/step-up), customer notification (careful!), audit',
   'accountId', 'fraud.alerts.v1 keyed by accountId — an account\'s alert stream stays ordered for velocity logic',
   'alertId, accountId, paymentRef?, score, reasonCodes, modelVersion, createdAt',
   'Reason codes reveal detection logic — restricted topic; customer messaging is a separate, careful contract (no tipping fraudsters)',
   'Per-account order for cumulative-risk consumers',
   'alertId unique; case creation one-per-alert as with hits',
   'Fast lane — alerts age badly; short retries then page',
   'Alert + model version + features hash audited for model-governance evidence',
   'modelVersion mandatory field; reasonCodes additive',
   'Alerts vs cases; alert-rate vs model-version changes (drift detection)']
  ];

  D.bankingEvents = E.map(function (e) {
    return { name: e[0], domain: e[1], producer: e[2], consumers: e[3], key: e[4], partition: e[5],
      payload: e[6], sensitive: e[7], ordering: e[8], idempotency: e[9], retry: e[10],
      audit: e[11], evolution: e[12], reconciliation: e[13] };
  });

  /* ---------- Kafka decision guides ---------- */
  D.kafkaDecisions = [
    { q: 'Kafka versus REST', a:
`Ask: does the caller need an answer **now** to proceed? REST. Is it a **fact** that multiple consumers need, now and in the future, surviving their outages? Kafka. Most domains use both: REST for queries/commands (mandate check before release), Kafka for propagation (customer changed). The classic error is forcing one to do the other's job — request/response over Kafka is contortion; propagating state via polling REST is a coupling and freshness tax.` },
    { q: 'Kafka versus a message queue', a:
`Kafka is a **retained log** (consumption doesn't delete; many groups read independently; replay exists). Queues excel at **work distribution** with per-message acknowledgement and per-message DLQ semantics. One consumer executing jobs → a queue is simpler. Many consumers of the same facts, replay needs, or an integration backbone → Kafka. Retention, fan-out and replay are the deciding properties, not throughput folklore.` },
    { q: 'Event versus command', a:
`Events state facts (past tense, producer-owned, no expectation): *CustomerUpdated*. Commands request actions (directed, expect handling): *ScreenCustomer*. Coupling directions are opposite. The smell: an "event" published so that a specific service acts ("OrderNeedsScreening") — that's a command in costume. Name it honestly or restructure so the consumer subscribes to genuine facts.` },
    { q: 'Domain event versus integration event', a:
`Domain events are rich, internal to a bounded context, free to change with the model. Integration events are **published contracts** across contexts: versioned, schema-governed, minimal. Never leak internal domain events as enterprise contracts — translate at the boundary. The event catalogue governs integration events only; domain events are the producing team's business.` },
    { q: 'Event notification versus event-carried state transfer', a:
`Notification: ID + type, consumers call back — small, fresh, low leak risk, but creates read-traffic and availability coupling to the producer's API. State transfer: payload carries the data — autonomous consumers, but staleness, versioning burden and **sensitive-data spread** (a leaked topic = a leaked database). Choose per event type; hybrid (key fields + reference) is legitimate. In banking, PII pushes toward notification or restricted streams.` },
    { q: 'Orchestration versus choreography', a:
`Choreography (services react to events) for short, stable, 2–3 step flows. Orchestration (explicit coordinator with state) when steps ≥ 4, when compensation logic is rich, or when humans/auditors will ask "where is payment 123?" — in banking that's most money-touching workflows. The orchestrator must not absorb domain logic from participants; it coordinates, they decide.` },
    { q: 'Topic per event type versus topic per domain stream', a:
`Per event type: fine-grained ACLs and retention, but consumers lose cross-type ordering per entity. Per domain stream (customer.events.v1, type in payload): preserves per-entity lifecycle ordering — usually right for entity lifecycles — at the cost of coarser ACLs and consumer-side filtering. Decide per domain; banking party/payment lifecycles almost always want the stream.` },
    { q: 'Retry topic versus dead-letter handling', a:
`They answer different failures: retry topics buy **time** for transient outages (vendor down 10 min) without blocking the partition; DLTs park **permanent** failures (poison messages) for human triage and replay. Use both, tiered: in-memory retries → retry.5m → retry.1h → DLT. The caveat to state aloud: retry topics sacrifice per-key ordering — on ordered streams (payment state machines) use block-and-alert instead.` },
    { q: 'Replay versus database correction', a:
`Replay when the **log is right and the projection is wrong** (rebuild the index, recover a bad consumer deploy). Correct the database (with audit, possibly via correction events) when the **events themselves were wrong** — and emit corrections rather than editing history. Precondition for replay: idempotent consumers with dedupe windows ≥ replay horizon; otherwise the replay is the next incident.` },
    { q: 'One consumer group versus multiple groups', a:
`One group per **logical consumer** (search indexer, fraud, audit are three groups): independent offsets, pace, failure isolation, and replay per consumer. Multiple instances scale **within** one group up to partition count. Sharing one group across different purposes makes them compete for messages — a correctness bug, not an optimisation.` },
    { q: 'Partitioning strategy', a:
`Partition count from peak throughput ÷ per-partition consumer throughput × headroom (≈2×) — and generous, because increasing later remaps keys and breaks per-key order around the change. Check the key's cardinality and skew (hot customers). Max useful group parallelism = partitions. See the [partition calculator](#/calculators).` },
    { q: 'Event key selection', a:
`Key = the entity whose event order consumers need: partyId for customer lifecycles, accountId for postings, paymentId for payment state machines. Requirements: **stable** (never emails), **high-cardinality**, skew-aware. The key also enables compaction and defines the replay/dedupe scope. This is the single most consequential design choice per topic — treat it as such in reviews.` }
  ];
})(window.SAA.data);
