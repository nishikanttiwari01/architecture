/* interview-banking.js — banking-specific interview questions (same compact format as interview.js) */
(function (D) {
  'use strict';
  var BSA = 'Banking Software Architect', BSO = 'Banking Solution Architect', PAY = 'Payments Architect',
      FCA = 'Financial Crime Technology Architect', BDA = 'Banking Data Architect',
      KA = 'Kafka Architect', MA = 'Microservices Architect', JA = 'Java Application Architect', SEC = 'Security Architect';

  var Q = [
  /* ---- Banking domain fundamentals ---- */
  ['bk1',[BSA,BSO],'Banking domain','Intermediate','Explain the difference between party, customer, and partner — and why it matters architecturally.',
   'Banking entity precision; boundary awareness.','Definitions → per-system divergence → glossary-first discipline → boundary consequence.',
   [['Party as umbrella; customer = party with relationship; partner = related party','party','customer','partner','umbrella'],['Definitions differ per system/context','differ','crm','context'],['Domain glossary before boundaries','glossary','define','first'],['Wrong terms → two masters → reconciliation pain','master','boundary','reconcil']],
   'Party is any known person/organisation; a customer is a party with a banking relationship; partners are related parties — UBOs, directors, signatories — screened without being customers. Every system defines these differently, so I build the domain glossary first; boundaries drawn on ambiguous terms put one entity under two masters.',
   'Treating customer as the root entity everywhere.',
   ['Model a holding company with a 60% owner-director.'],['b2']],
  ['bk2',[BSA,BSO],'Banking domain','Intermediate','Walk me through what happens between a customer clicking "pay" and the money arriving.',
   'Payment pipeline fluency.','Initiation → validation → fraud/sanctions → reservation → clearing → settlement → posting → notification/recon.',
   [['Idempotency key at initiation','idempotency','key','initiat'],['Fraud and sanctions checks in-line','fraud','sanction','screen'],['Reservation before clearing','reserv','balance','double'],['Clearing vs settlement distinction','clearing','settlement','scheme'],['Posting + reconciliation close the loop','posting','ledger','reconcil']],
   'Initiation (with an idempotency key from hop one) → validation → fraud scoring and sanctions screening → balance check and reservation (the anti-double-spend control) → clearing through the scheme → settlement (irrevocable, often central-bank money) → ledger posting → notifications, and daily reconciliation against scheme records. The payment is a state machine; "unknown after timeout" is a designed state.',
   'Conflating clearing with settlement; no mention of reservation.',
   ['Where can this pipeline produce duplicates, and what stops each?'],['b4']],
  ['bk3',[BSA,BSO,FCA],'Banking domain','Intermediate','What is maker-checker and how would you implement it properly?',
   'Control implementation depth.','Concept → pending states in domain → backend enforcement → audit of both identities.',
   [['Segregation of duties concept','segregation','initiator','approver','different'],['Pending-approval state machine','pending','state','workflow'],['Backend enforcement, never UI-only','backend','api','bypass','ui only'],['Both identities in the audit trail','audit','both','identities']],
   'A sensitive action (large payment, mandate change, hit release) is created in a pending state by the maker and only takes effect when a different, authorised checker approves — enforced in the backend state machine (maker ≠ checker asserted server-side), with both identities, timestamps and the data version recorded immutably.',
   'A confirmation dialog described as four-eyes.',
   ['How do you stop the maker approving via the API directly?'],['b3']],
  ['bk4',[BSA,BDA],'Banking domain','Advanced','Design a complete audit trail for a banking platform.',
   'Auditability as architecture.','Business-fact events → immutability/integrity → central audit domain → retention & reconstruction.',
   [['Audit events as business facts with actor/authority/data version','who','what','when','version','fact'],['Append-only, integrity-protected store','immutable','append','hash','worm'],['Audit as a separate consumed domain','separate','domain','contract','sink'],['Retention 7-10y, survives system replacement, reconstructible','retention','years','reconstruct','survive']],
   'Every service emits audit events — business facts with actor, authority (mandate/role), timestamp, and the data/list versions used — to a dedicated append-only audit domain (integrity-protected, WORM where mandated). Making audit a consumed contract means no service can "forget" to audit. Retention 7–10+ years, surviving system replacement, with reconstruction queries ("why was this decided?") as a tested capability.',
   'Grepping application logs and calling it audit.',
   ['Show how you would prove why a screening decision was made two years ago.'],['b3','a9']],
  ['bk5',[BSA,BSO],'Banking domain','Advanced','When should a banking system remain a modular monolith?',
   'Judgement against fashion.','Consistency needs → team shape → the ledger example → extraction triggers.',
   [['Strong consistency favours one deployable','consistency','acid','transaction'],['Single team / cognitive load','team','cognitive'],['Ledger core as the canonical example','ledger','posting','core'],['Extract at the edges under real pressure','edge','extract','trigger']],
   'When the domain\'s invariants genuinely need transactional consistency (ledger posting), when one team owns it, and when no divergent scaling or isolation pressure exists. A common healthy shape: a strongly consistent modular core for posting with services extracted at the edges (screening, notification) as real pressures appear.',
   '"Monoliths are legacy" reflexes.',
   ['Which module would you extract first from a banking core satellite, and why?'],['d9','b6']],
  ['bk6',[BSA,BSO,MA],'Banking domain','Expert','Define boundaries for customer, account, relationship, payment and screening domains.',
   'Applied banking decomposition.','Per-domain ownership → why relationship is separate → consistency placement → contracts between.',
   [['Each domain masters its data','master','own','data ownership'],['Relationship separate from party (cadence, graph shape, consumers)','relationship','separate','graph','ubo'],['Strong consistency inside, events between','inside','eventual','events between'],['Sync only where the answer gates action (mandate check)','mandate','sync','gate']],
   'Party/customer masters identity and KYC status; relationship is its own domain — graph-shaped UBO/role data with a different change cadence and consumer set; account wraps the core with mandates; payment owns the state machine; screening owns requests/results/cases. Strong consistency lives inside each; between them, integration events with outbox/inbox — with a handful of gated sync calls (mandate check before release) protected by breakers and defined degraded modes.',
   'One "customer service" owning everything customer-adjacent.',
   ['Where do you accept eventual consistency and where never?'],['b6','b2']],
  ['bk7',[BSA,BDA],'Banking domain','Advanced','How would you design evidence retention for screening cases?',
   'Regulatory evidence engineering.','What evidence → versioned inputs → immutable case history → retention/retrieval design.',
   [['Evidence = input data, list version, matches, analyst view, decision, authority','list version','decision','analyst','input'],['Immutable case history (event-sourced or append-only)','immutable','append','event'],['Retention beyond relationship end, retrievable','retention','years','retriev'],['Reconstruction as a tested query','reconstruct','as of','prove']],
   'Retain what reconstructs the decision: the party data as screened, the exact list version, raw matches and scores, what the analyst saw, the decision and its authority (four-eyes identities). Case history is append-only or event-sourced; retention outlives the relationship by the regulatory period; and "show me why case 4711 was closed in 2023" is a rehearsed query, not an archaeology project.',
   'Storing only the final decision flag.',
   ['A regulator asks for all decisions made under list version X — how does your design answer?'],['b5','b3']],
  ['bk8',[FCA,BSA],'Banking domain','Intermediate','Sanctions screening vs transaction monitoring vs fraud detection — distinguish them.',
   'Financial-crime domain map.','Lists vs behaviour vs loss-prevention → timing → actions → architecture consequences.',
   [['Screening = matching against lists','list','sanction','match'],['Monitoring = behavioural patterns over transactions','pattern','behaviour','monitor'],['Fraud = real-time loss prevention','fraud','real-time','block'],['Different latency/action profiles drive different designs','latency','block','alert','batch']],
   'Screening matches parties/payments against sanction/PEP lists — blocking, evidence-heavy. Transaction monitoring detects suspicious behaviour patterns (structuring) over time — alert/SAR-generating, mostly near-time or batch. Fraud detection prevents losses in real time on the payment path — milliseconds, scoring, step-up. Three different latency/action profiles, three different architectures.',
   'Using the three terms interchangeably.',
   ['Which one belongs synchronously in the payment path, and why?'],['b5','b4']],

  /* ---- Consistency / data ---- */
  ['bk9',[BSA,KA,BDA],'Banking domain','Advanced','How would you maintain consistency between Oracle and Kafka?',
   'THE banking event-architecture question.','No shared transaction → outbox → inbox → reconciliation → both failure orders.',
   [['Dual-write problem: no atomic pair','dual-write','atomic','cannot'],['Transactional outbox with relay/CDC','outbox','debezium','relay','cdc'],['Idempotent consumers / inbox','inbox','idempotent'],['Reconciliation as audit-grade backstop','reconcil','backstop','compare'],['Both failure orders explained','db first','publish first','crash']],
   'They cannot share a transaction. DB-then-publish loses events on crash (silent divergence); publish-then-DB announces facts that never happened. So: transactional outbox — state and event commit atomically in Oracle, a relay (Debezium CDC or poller) publishes at-least-once; consumers dedupe via inbox tables; and a reconciliation job comparing state changes to published events is the control regulators actually examine.',
   'XA/2PC between Oracle and Kafka, or "we write to both carefully".',
   ['What do you monitor on the outbox relay?'],['b7','d2']],
  ['bk10',[BSA,KA],'Banking domain','Advanced','When would you use the transactional outbox pattern — and when not?',
   'Pattern judgement, not recitation.','Trigger condition → mechanics → costs → alternatives (CDC direct, no-event cases).',
   [['Trigger: state change + reliable event, same fate','state','event','atomic','fate'],['At-least-once ⇒ consumer idempotency','at-least-once','idempotent'],['Cost: relay ops, housekeeping, latency','relay','housekeep','latency'],['Alternatives: CDC on business tables, or no event needed','cdc','alternative','not needed']],
   'Use it whenever a state change and its announcement must share fate — payments posted, screening requested. Not needed for fire-and-forget telemetry, or when CDC on the business tables is itself the chosen (transformed!) contract, or when nobody actually consumes the event — the cheapest event is the one you don\'t publish. Costs: relay operations, outbox housekeeping, and consumers must still dedupe.',
   'Outbox on every table by policy.',
   ['Poller vs CDC as the relay — trade-offs?'],['b7','d2']],
  ['bk11',[BDA,BSA],'Banking domain','Advanced','Design reconciliation between two banking systems.',
   'Reconciliation as designed control.','Datasets & granularity → frequency → break workflow → evidence.',
   [['Define compared datasets and keys','dataset','compare','key'],['Granularity ladder: counts → totals → record-level','count','total','record'],['Break workflow with owner and repair audit','break','owner','workflow','repair'],['Frequency & timeliness vs detection window','daily','hourly','window']],
   'Define exactly what is compared (payments posted vs events published; our ledger vs the scheme file), at increasing granularity — counts, then business totals, then record-level on discrepancy. Daily minimum, hourly for payment flows. Every break enters an owned investigation workflow whose repairs are themselves audited (four-eyes for financial corrections). A reconciliation without a break-workflow is a dashboard.',
   'A nightly count query with no follow-up process.',
   ['How fast must a missed-screening-event break surface, and why?'],['b7','b5']],
  ['bk12',[BDA,BSA],'Banking domain','Intermediate','What is a system of record, and what goes wrong with two of them?',
   'Data-ownership fundamentals.','Definition → derived copies → dual-master failure modes → banking examples.',
   [['One authoritative home per entity','authoritative','one','master'],['All other copies derived and rebuildable','derived','rebuild','copy'],['Dual-master divergence + reconciliation load','diverge','conflict','two masters'],['Banking example (party, accounts)','party','core','example']],
   'The system of record is the single authoritative home of an entity — party master for identity, the core for accounts; everything else is a derived, rebuildable copy fed by events/CDC. Two masters diverge immediately: conflicting updates, no defensible answer for regulators, and permanent reconciliation load. During migrations, dual-mastering is the thing to design away hardest.',
   '"The warehouse is also kind of a master."',
   ['During a strangler migration, who masters the entity mid-flight?'],['b7','f5']],
  ['bk13',[BDA,BSA],'Banking domain','Advanced','How do you handle data residency in a multi-jurisdiction bank?',
   'Residency-constrained architecture.','Residency as early driver → regional pinning → split models → key custody nuance.',
   [['Residency discovered early, shapes deployment','early','driver','shape'],['Regional deployments with data pinned','region','pin','local'],['Global directory + local detail split','directory','split','global','local'],['Key custody / access controls as complements','key','custody','access','staff']],
   'Treat residency as a first-order driver — it redraws the deployment map. Patterns: regional stacks with customer data pinned to home jurisdiction (aligning write-partitioning with residency); a thin global directory holding only non-restricted reference keys while details stay local; access controls restricting which staff see which jurisdiction; and key-custody arrangements where law permits data abroad but keys at home — with legal sign-off, not architect optimism.',
   'Adding residency filters after the global design is finished.',
   ['A global fraud model needs features from all regions — options?'],['dt2','b3']],
  ['bk14',[BDA],'Banking domain','Intermediate','Why do banks still run so much batch, and how do you design around it?',
   'Operational calendar realism.','EOD dependency graphs → cut-offs → online/batch coexistence → instant rails exception.',
   [['EOD/month-end as dependency-ordered graphs','eod','end of day','dependency','graph'],['Cut-off times shift value dates','cut-off','value date'],['Online degraded/read-only modes during batch','degrade','read-only','window'],['Restartable, idempotent, monitored jobs','restart','checkpoint','idempotent']],
   'Interest accrual, statements, GL posting and regulatory feeds are naturally periodic and volume-efficient as batch. Design consequences: explicit dependency graphs with late-start (not just failure) alerts; restartable checkpointed jobs (Spring Batch); online services with defined behaviour during windows; and capacity sized to month-end, not Tuesday. Instant rails then run 24/7 through all of it — both worlds, deliberately.',
   'Pretending batch is legacy to be ignored.',
   ['The 02:00 job feeding a 06:00 regulatory report fails. Walk your design\'s response.'],['b9','j2']],

  /* ---- Screening / financial crime ---- */
  ['bk15',[FCA,BSA],'Banking domain','Expert','Design a customer and partner screening platform end to end.',
   'The flagship fincrime design question.','Triggers → event-driven core → vendor integration → case workflow → evidence → failure drills.',
   [['Three triggers: onboarding, data changes, list updates','onboard','change','list update','trigger'],['Event-driven core with outbox/inbox','event','outbox','inbox'],['Vendor via ACL + resilience + queue-on-outage','vendor','acl','breaker','queue'],['Hit→case→task workflow, idempotent, four-eyes','case','task','idempotent','four-eyes'],['Evidence: list versions, immutable history','evidence','list version','immutable']],
   'Party/relationship domains publish lifecycle events; a screening-orchestration service consumes (inbox-deduped), determines scope (partners via relationship traversal), persists requests with outbox, and calls the vendor behind an ACL with timeout/breaker/queue-on-outage. Results flow to case management — one case per hit, idempotent — with four-eyes release, Elasticsearch read model for investigators, restartable batch lanes for list-update re-screening, and evidence (data + list version + decision + authority) retained immutably for years.',
   'A synchronous vendor call from the onboarding form.',
   ['Which failure scenario worries you most and how does the design answer it?'],['b5']],
  ['bk16',[FCA,KA],'Banking domain','Advanced','How would you detect that a customer update event was missed by screening?',
   'Missed-event detection — a spec-listed drill.','Lag alerts (leading) → count reconciliation (trailing) → full-sync backstop → repair path.',
   [['Consumer-lag and DLT alerting as leading signals','lag','dlt','alert'],['Windowed count reconciliation: changes vs requests','count','window','compare','reconcil'],['Periodic full-state comparison backstop','full','sync','snapshot'],['Repair: targeted re-screen with audit','re-screen','repair','audit']],
   'Three layers: leading — consumer-lag growth and DLT alerts; trailing — a reconciliation comparing party-change counts to screening-request counts per window (with tolerance for in-flight); backstop — periodic full comparison of latest-screened-version vs current-party-version per entity. Detected gaps trigger targeted re-screening, audited as such so the evidence trail shows the miss and the repair.',
   'Trusting Kafka to be reliable enough.',
   ['Why do you need the trailing layer if lag alerting exists?'],['b5','b7']],
  ['bk17',[FCA,KA],'Banking domain','Advanced','A replay creates duplicate screening requests. Prevent and repair.',
   'Replay-safety engineering.','Idempotent request creation on business key → dedupe window vs replay horizon → repair with audit.',
   [['Business-key idempotency: party+trigger+listVersion','business key','party','list version','unique'],['Dedupe retention ≥ replay horizon','window','horizon','retention'],['Repair: cancel duplicates with audit, keep evidence coherent','cancel','repair','audit'],['Replay preconditions documented','precondition','idempotent','runbook']],
   'Prevention: request creation idempotent on the business key (partyId + trigger + list version), not the message ID — replays mint new message IDs; dedupe state retained at least as long as replays are possible. Repair: identify duplicates by business key, cancel extras with an audited reason, and verify no duplicate cases spawned downstream. Replay runbooks list idempotency as a precondition — replaying into a non-idempotent consumer *creates* the incident.',
   'Deduping on Kafka message ID only.',
   ['Why is the message ID insufficient here?'],['b5','k4']],
  ['bk18',[FCA,BSA],'Banking domain','Advanced','The screening vendor is down. Walk me through your design\'s behaviour, minute by minute.',
   'Vendor-outage resilience with business awareness.','Breaker opens → durable queue → business policy per flow → drain + monitoring → evidence.',
   [['Breaker + timeout detect fast','breaker','timeout','open'],['Durable queue buffers requests, nothing dropped','queue','durable','buffer'],['Pre-agreed business policy: block vs provisional per flow','policy','block','provisional','business'],['Drain with rate control on recovery + backlog monitoring','drain','rate','backlog']],
   'Minute 1: timeouts trip the breaker; requests stop hitting the vendor and buffer durably — nothing is silently skipped. Interactive onboarding follows the pre-agreed policy: block, or provisional-with-hold where the business signed that off. Batch lanes pause. Recovery: drain the backlog rate-limited (their recovery is fragile), monitor age-of-oldest-request, and the audit trail shows every decision\'s actual screening time.',
   'Skip screening during the outage "to keep onboarding fast".',
   ['Who decides block-vs-provisional and when?'],['b5','d4']],
  ['bk19',[FCA],'Banking domain','Advanced','How would you re-screen customers after a sanctions-list update?',
   'List-driven re-screening at scale.','Delta scoping → restartable batch → capacity isolation → evidence per decision.',
   [['Delta screening scoped by list changes','delta','changed','scope'],['Restartable, checkpointed batch (fails midway safely)','restart','checkpoint','batch','midway'],['Bulkheaded capacity: batch lane vs interactive lane','lane','capacity','isolat','interactive'],['List version recorded on every decision','version','record','evidence']],
   'Scope by delta — which list entries changed and which parties could match them — rather than brute-forcing the book where the vendor supports it; otherwise the full book runs as a checkpointed, restartable batch (a midway failure resumes, never half-applies silently). Batch capacity is bulkheaded from interactive screening so onboarding stays fast. Every decision records the exact list version — the evidence regulators ask for.',
   'A full-book rescreen in the online lane with no checkpointing.',
   ['The list update itself fails midway — what must be true of your ingestion?'],['b5','j2']],
  ['bk20',[FCA,BSA],'Banking domain','Advanced','One screening hit, multiple investigation tasks created. And its mirror: a hit with no task. Handle both.',
   'Workflow integrity engineering.','Idempotent creation (uniqueness) → hit↔case reconciliation → repair semantics.',
   [['Uniqueness on hitId for case/task creation','unique','hitid','constraint','idempotent'],['Inbox dedupe on the consumer','inbox','dedupe'],['Hit↔case reconciliation catches both directions','reconcil','orphan','both'],['Repairs audited; merged tasks preserve evidence','merge','repair','audit']],
   'Prevention: case/task creation idempotent — a uniqueness constraint on hitId plus inbox dedupe, so redelivery finds the existing case. Detection: a reconciliation comparing hits to cases in both directions — orphan hits (compliance exposure: a match nobody is investigating) page someone; duplicate tasks get merged with evidence preserved and the merge audited. The orphan direction is the dangerous one; say so.',
   'Treating duplicates as cosmetic.',
   ['Which direction is the regulatory risk and why?'],['b5']],
  ['bk21',[FCA],'Banking domain','Intermediate','How do you manage false positives without missing true hits?',
   'Precision/recall judgement in compliance.','Scoring/rules reduction → model governance → thresholds as business risk decisions → feedback loop.',
   [['Scoring and rule-based reduction before humans','score','rule','reduce'],['Versioned, governed matching logic (explainable)','version','governance','explain'],['Thresholds are risk decisions owned by compliance','threshold','risk appetite','compliance'],['Analyst-feedback loop tunes the system','feedback','tune','loop']],
   'Layered reduction: exact-data disqualifiers (DOB, nationality mismatch), scoring, then humans — with every rule and threshold versioned and explainable, because "the model cleared it" must survive a regulator. Thresholds are risk-appetite decisions signed by compliance, not engineering tuning knobs. Analyst outcomes feed back to measure and improve precision without silently degrading recall — chasing zero false positives buys false negatives.',
   'Engineering unilaterally raising thresholds to cut queue size.',
   ['A threshold change is proposed — what is the governance path?'],['b5','m10']],
  ['bk22',[FCA,BSA],'Banking domain','Advanced','A relationship expires but downstream screening remains active. Design the fix.',
   'Lifecycle propagation integrity.','Expiry events consumed → scope recomputation → full-sync backstop → audit of descoping.',
   [['RelationshipExpired event consumed by screening','expired','event','consume'],['Screening scope recomputed on relationship change','scope','recompute','descope'],['Periodic full-sync catches missed expirations','full sync','periodic','backstop'],['Descoping itself audited (why screening stopped)','audit','stop','reason']],
   'Screening scope derives from relationships, so RelationshipExpired events must trigger scope recomputation — the partner may leave screening if no other active relationship justifies it (they often have several; recompute, don\'t just remove). Backstop: periodic full comparison of active screening scopes vs current relationship state. Descoping is itself audited — "why did we stop screening X" is a question you will be asked.',
   'Screening scope as a one-time onboarding snapshot.',
   ['A partner has three relationships; one expires. What happens?'],['b5','b2']],

  /* ---- Payments ---- */
  ['bk23',[PAY,BSA],'Banking domain','Advanced','How would you prevent duplicate payment processing?',
   'Layered duplicate defence — spec-listed.','Idempotency keys → business dedupe window → idempotent consumers per hop → scheme/recon backstop.',
   [['Client idempotency key stored with result, same tx','idempotency key','store','transaction'],['Business-level dedupe (debtor+creditor+amount+ref window)','business','window','same amount','resubmi'],['Idempotent consumers on every event hop','consumer','hop','inbox'],['Reconciliation catches what slips','reconcil','backstop']],
   'Layers: an Idempotency-Key at the API whose key+result commit with the payment; business dedupe holding same-debtor/creditor/amount/reference within a window (catches file re-submissions the technical key misses); inbox-based idempotent consumers at every event hop; and daily reconciliation against scheme records as the audit-grade net. One layer is a hope; four are a design.',
   'One UUID check at the edge as the whole answer.',
   ['Same key, different amount — response code and why?'],['b4','d3']],
  ['bk24',[PAY,BSA],'Banking domain','Expert','Design a real-time (instant) payment system.',
   'Instant-rail architecture.','Seconds-SLA pipeline → inline screening strategy → 24/7 ops → reservation & posting under concurrency.',
   [['End-to-end seconds SLA incl. scheme timeout','seconds','sla','timeout','scheme'],['Inline fraud/sanctions with pre-computation/tight budgets','inline','pre-comput','budget','screen'],['24/7: zero-downtime deploys, no batch windows','24/7','zero-downtime','no window'],['Reservation + idempotent posting under concurrency','reserv','concurren','idempotent'],['Confirm-or-reject discipline, no unknown to customer','confirm','reject','status']],
   'The scheme gives a hard confirm-or-reject window (seconds). The pipeline is minimal-synchronous: validation, fraud scoring and sanctions screening with strict latency budgets (pre-computed party screening status where policy allows, tight vendor SLAs), reservation, scheme submission, posting. Everything is idempotent; every step is bounded; the platform deploys zero-downtime because instant rails never close. Timeouts resolve to a definitive state via scheme status inquiry — customers never see "maybe".',
   'Adapting a batch pipeline with faster cron.',
   ['Where does the sanctions check sit and what is its latency budget?'],['b4','cs9']],
  ['bk25',[PAY],'Banking domain','Intermediate','Explain reversals, returns, refunds and chargebacks — and their shared architectural principle.',
   'Compensation semantics in payments.','Definitions → who initiates → the never-edit principle → linkage & audit.',
   [['Distinct initiators/flows for each','reversal','return','refund','chargeback'],['All are new compensating transactions','new transaction','compensat','never edit'],['Linkage to the original preserved','link','reference','original'],['Ledger immutability behind it all','ledger','immutable','append']],
   'A reversal undoes an erroneous entry (bank-initiated); a return comes back from the receiving side (account closed); a refund is merchant/customer-service-initiated; a chargeback is a scheme-arbitrated card dispute with evidence deadlines. The shared principle: every one is a *new* transaction referencing the original — nothing is ever edited or deleted, because the ledger is append-only and the audit trail is the product.',
   'Implementing any of them as an UPDATE.',
   ['Model the states a disputed card payment moves through.'],['b4']],
  ['bk26',[PAY,KA],'Banking domain','Advanced','How would you manage retries without duplicating financial transactions?',
   'Retry-safety for money — spec-listed.','Idempotency precondition → retry budget/backoff → unknown-outcome resolution → recon.',
   [['Retry only idempotent operations (keys first)','idempotent','key','precondition'],['Backoff + jitter + budget','backoff','jitter','budget'],['Timeout = unknown: query status before blind retry','unknown','status','inquiry','query'],['Reconciliation as the final net','reconcil']],
   'Retries are only safe against idempotent operations — the idempotency key comes first, then the retry policy (backoff, jitter, bounded attempts). A timeout means *unknown*, not failed: where the counterparty offers a status inquiry, query before re-submitting. Every hop that forwards a payment carries the same key so the whole chain dedupes, and reconciliation catches the residue.',
   'Aggressive retries with a "duplicates are rare" shrug.',
   ['The scheme has no status API and your submit timed out. Options?'],['b4','d3']],
  ['bk27',[PAY,BSA],'Banking domain','Intermediate','What do ISO 20022 and its structured data change architecturally?',
   'Standards awareness with consequences.','Message families → structured party/remittance data → screening/recon benefits → translation layers.',
   [['Message families (pain/pacs/camt) correctly placed','pain','pacs','camt'],['Structured data improves screening & STP','structured','screening','stp','remittance'],['Recon leverage from camt statements','camt','statement','reconcil'],['Translation/coexistence with legacy MT','mt','translate','coexist']],
   'ISO 20022 gives structured, rich messages: pain.001 initiation, pacs.008 interbank, camt.05x statements. Structured debtor/creditor and remittance data improves sanctions-screening precision (fewer false positives than free text), straight-through processing, and reconciliation automation. Architecturally you gain data but must run translation/coexistence layers while legacy MT formats persist — an ACL job, not a string mapping.',
   '"It is just XML."',
   ['Where does the richer data concretely reduce false positives?'],['b4']],
  ['bk28',[PAY,BSA],'Banking domain','Advanced','Payments must keep flowing during the core banking system\'s nightly batch. Design it.',
   'Coexistence engineering.','ODS/shadow balances → queue-and-forward posting → limits policy → morning reconciliation.',
   [['Shadow balance / ODS serves during window','shadow','ods','balance','window'],['Accept-and-queue posting until core reopens','queue','forward','post later'],['Risk policy: limits during degraded authorisation','limit','policy','risk'],['Reconciliation on window close','reconcil','morning','catch up']],
   'During the window, authorisation runs on a shadow-balance view (ODS updated by the day\'s flows) with a business-agreed risk policy — possibly tighter limits since the view is "as of" the window start. Accepted payments queue durably and post when the core reopens, idempotently and in order per account. Window-close reconciliation proves the shadow view and the core converge. The design is a business risk decision wearing architecture clothes — say so.',
   'Declining all payments nightly, or double-posting at reopen.',
   ['Who signs off the degraded-mode limit policy?'],['b9','b4']],

  /* ---- Kafka banking ---- */
  ['bk29',[KA,BSA],'Kafka','Advanced','How would you partition Kafka events for a customer domain?',
   'Applied partitioning — spec-listed.','Key = customerId/partyId → ordering rationale → count sizing → hot-key handling.',
   [['Key on the stable party/customer ID','customerid','partyid','key'],['Per-customer ordering across event types','order','per customer','lifecycle'],['Partition count from throughput + growth headroom','count','throughput','headroom'],['Hot keys and repartition consequences addressed','hot','skew','remap']],
   'Key by partyId — stable, high-cardinality, and the scope consumers need ordered (a customer\'s create → update → address-change must not interleave wrongly). One domain-stream topic (customer.events.v1) preserves cross-type ordering per customer. Partitions sized from peak MB/s with 2× headroom because increasing later remaps keys. Watch hot keys (large corporates) and capacity-plan for them explicitly.',
   'Keying on event type or timestamp.',
   ['Topic per event type vs one domain stream — argue it for this domain.'],['k1','b6']],
  ['bk30',[KA,BSA],'Kafka','Advanced','How would you evolve banking event schemas safely?',
   'Schema-evolution discipline — spec-listed.','Registry + FULL compatibility → additive-only in-version → new major topic for breaks → consumer standards.',
   [['Schema Registry enforcing compatibility in CI','registry','compatib','ci','reject'],['Additive-only with defaults within a version','additive','optional','default'],['Breaking = new major topic + migration + retirement','v2','major','parallel','retire'],['Consumers tolerate unknown fields (coding standard)','unknown fields','tolerant']],
   'Avro/Protobuf schemas in a registry with FULL compatibility, enforced at CI so breakage fails builds, not consumers at 2 a.m. Within a version: additive optional fields with defaults only. Breaking changes get customer.events.v2 run in parallel — dual-publish or transform — with lag-monitored consumer migration and a retirement date. Consumers ignoring unknown fields is a coding standard, verified by contract tests.',
   'In-place field renames coordinated by email.',
   ['Long-retention topics: what extra burden does evolution carry?'],['k3']],
  ['bk31',[KA,FCA],'Kafka','Advanced','How would you handle out-of-order customer events in screening?',
   'Ordering defence — spec-listed.','Per-key ordering usually suffices → residual reorder sources → version checks → stale-decision handling.',
   [['Keying gives per-customer order normally','key','partition','normally'],['Residual sources: retry topics, replays, producer config','retry topic','replay','residual'],['Version/sequence check in consumer: ignore stale','version','sequence','stale','ignore'],['Re-screen on correction rather than corrupt state','re-screen','correction']],
   'Correct keying makes per-customer order the norm; residual reordering comes from retry topics, replays, or misconfigured producers. The consumer defends itself: each event carries an entity version/sequence; a v5 arriving after v6 is ignored as stale (audited), never applied. If a stale-but-material update is detected late, trigger a scoped re-screen rather than silently overwriting — the evidence trail must show what was screened against what.',
   'Assuming Kafka ordering solves it everywhere.',
   ['Where do the version numbers come from?'],['d3','b5']],
  ['bk32',[KA,BSA],'Kafka','Intermediate','How would you handle duplicate Kafka events in a banking consumer?',
   'Consumer idempotency — spec-listed.','Duplicates are guaranteed → inbox in same tx → business-key layer → replay-window sizing.',
   [['At-least-once means duplicates, always','at-least-once','guarantee','normal'],['Inbox table in the same transaction as effects','inbox','same transaction'],['Business-key idempotency for replays','business key','replay'],['Retention ≥ replay horizon','retention','horizon','window']],
   'Duplicates are a when, not an if: rebalances, redeliveries, replays. The consumer records processed message IDs in an inbox table committed with the business effect; redeliveries hit the inbox and skip. Because replays mint new message IDs, a second layer dedupes on business keys (paymentId, hitId). Dedupe retention matches the replay horizon — a five-minute window against day-old replays is decoration.',
   'An in-memory seen-set.',
   ['Why two layers rather than one?'],['d3','k2']],
  ['bk33',[KA,BSA],'Kafka','Intermediate','How do you prevent sensitive data from leaking into logs and events?',
   'Data-protection plumbing — spec-listed.','Classification-driven → log allowlists at framework level → event payload minimisation/ACLs → detection.',
   [['Classification defines what may never appear','classif','never','pii'],['Framework-level masking + allowlisted structured fields','allowlist','mask','framework','structured'],['Events: IDs over payloads, ACLs, encryption for high-value fields','notification','acl','encrypt','minimal'],['Scanning/detection as fitness function','scan','detect','fitness']],
   'Start from data classification: what may never appear anywhere. Logs: structured logging with an *allowlist* of loggable fields enforced in the logging framework — denylists lose to developer creativity; masking for identifiers that must appear. Events: notification-style payloads (IDs, not PANs), topic ACLs, field-level encryption for the exceptions, short retention. Then verify: log scanners in CI and production as fitness functions, because policy without detection drifts.',
   '"Developers know not to log passwords."',
   ['Why does an allowlist beat a denylist here?'],['b3','k1']],

  /* ---- Java banking ---- */
  ['bk34',[JA,BSA],'Java & Spring','Advanced','How would you implement idempotency in Spring Boot for a banking API?',
   'Spec-listed implementation depth.','Key intake → unique-constraint storage with result, same tx → replay + conflict semantics → cleanup.',
   [['Idempotency-Key header, client-owned','header','client','idempotency-key'],['Unique constraint; key+hash+result stored in the effect\'s transaction','unique','constraint','same transaction','store result'],['Replay returns stored result; payload mismatch = 422','replay','mismatch','422','conflict'],['Concurrent duplicates resolved by the constraint; TTL housekeeping','concurrent','race','ttl']],
   'A filter/service intercepts the Idempotency-Key: insert (key, request-hash, status) under a unique constraint in the same transaction as the payment write; store the response on completion. Retries return the stored response; the same key with a different payload is rejected 422; concurrent duplicates race on the constraint and the loser returns the winner\'s result (or 409-retry while in-flight). TTL matches the retry horizon; the table is partitioned/purged accordingly.',
   'A Redis SETNX with no transactional tie to the effect.',
   ['Why must the key insert share the payment\'s transaction?'],['j3','d3']],
  ['bk35',[JA,BSA],'Java & Spring','Advanced','Design the transaction boundaries for a service that debits an account and publishes an event.',
   'Applied j2+outbox synthesis.','One use-case tx: debit + outbox → no remote calls inside → relay publishes → locking choice.',
   [['Single local transaction: debit + outbox row','debit','outbox','one transaction'],['No Kafka/HTTP inside the transaction','inside','remote','never'],['Relay publishes after commit, at-least-once','relay','after commit'],['Optimistic vs pessimistic on the balance row','optimistic','pessimistic','balance','lock']],
   'The application-service method opens one transaction: debit (balance row under FOR UPDATE or optimistic version, chosen by contention), insert ledger entries, insert outbox row — commit. No Kafka publish or HTTP call inside; the relay publishes after commit at-least-once and consumers dedupe. A crash before commit loses nothing; after commit, the relay guarantees eventual publication. This is lesson-b7\'s answer in code shape.',
   '@Transactional around a method that also calls kafkaTemplate.send().',
   ['Which locking did you pick for the balance row and why?'],['j2','b7']],
  ['bk36',[JA],'Java & Spring','Intermediate','Your nightly re-screening batch must process 3M parties and survive failures. Which Spring tools and design?',
   'Batch engineering for banking.','Spring Batch chunking → checkpoint/restart → skip/retry policies → parallelisation + observability.',
   [['Spring Batch chunk-oriented steps','spring batch','chunk'],['Checkpointed restart (fails midway → resumes)','restart','checkpoint','resume'],['Skip/retry policies with error records parked','skip','retry','policy'],['Partitioned/parallel steps + progress metrics','partition','parallel','grid','metrics']],
   'Spring Batch: chunk-oriented steps (read parties, screen, write results) with the job repository checkpointing progress — a 2 a.m. failure resumes from the last chunk, never re-runs 2M parties or half-applies silently. Retry policies for transient vendor errors, skip-with-park for poison records, partitioned steps for parallel throughput within the bulkheaded batch lane, and progress/ETA metrics because operations will ask.',
   'A while-loop over a JPA findAll().',
   ['How do you make the re-run idempotent at the screening-request level?'],['j2','b5']],

  /* ---- Security banking ---- */
  ['bk37',[SEC,BSA],'Security','Advanced','How would you secure communication between banking microservices?',
   'Spec-listed east-west security.','mTLS identity → authZ per call → NetworkPolicies → secrets + audit.',
   [['mTLS with workload identity (mesh at scale)','mtls','mesh','identity'],['Service-level authorisation, not just encryption','authoris','allowed to call','token'],['Default-deny network segmentation','default-deny','networkpolic'],['Short-lived secrets + call audit','vault','short-lived','audit']],
   'Encryption alone is half the job: mesh-issued mTLS gives every workload cryptographic identity; on top, service-level authorisation — which service may call which operation (token claims or mesh policy). NetworkPolicies default-deny so lateral movement dies. Secrets are vault-issued and short-lived; privileged data paths log caller identity for audit. That combination is what "Zero Trust" means when a regulator asks.',
   '"It is inside the cluster, so it is trusted."',
   ['Where does coarse authZ live vs fine-grained?'],['d15','d12']],
  ['bk38',[SEC,BSA],'Security','Intermediate','A developer needs production data to debug a screening issue. Response?',
   'Data-protection judgement under pressure.','Deny raw access → masked/synthetic paths → break-glass with controls → fix the observability gap.',
   [['No raw production PII to developers','no','deny','masked'],['Masked datasets / targeted redacted extracts','mask','synthetic','extract'],['Break-glass: time-bound, approved, recorded','break-glass','approved','recorded','time'],['Root cause: observability should have answered it','observab','logs','trace','gap']],
   'Raw production access is a no — banking secrecy and classification are not suspended by urgency. Paths: the masked non-prod dataset; a targeted, redacted extract of the specific case; or, if genuinely unavoidable, break-glass access — time-bound, four-eyes approved, session-recorded, audited. Then treat the request as a symptom: if debugging needs raw data, observability (correlation IDs, structured events) has a gap to fix.',
   '"Just this once, read-only."',
   ['What would have made this request unnecessary?'],['b3','a9']],
  ['bk39',[SEC,FCA],'Security','Advanced','Threat-model the screening platform: give me the top three threats and mitigations.',
   'Applied STRIDE on the flagship domain.','Boundaries → tampering with decisions → info disclosure of case data → repudiation/insider.',
   [['Tampering: altering screening results/thresholds','tamper','alter','threshold','integrity'],['Information disclosure: case/PII data breadth','disclos','pii','case','leak'],['Repudiation/insider: unlogged decisions, maker-checker bypass','repudiat','insider','bypass','maker'],['Mitigations tied to each: signing/immutability, ACL+masking, four-eyes+audit','immutable','acl','four-eyes']],
   'Top three: (1) tampering — altering match results or thresholds to wave someone through; mitigate with versioned, governed rules, immutable decision records, integrity checks. (2) information disclosure — case data and PII spread across topics/indices; mitigate with topic ACLs, field minimisation, masking, tight index access. (3) repudiation/insider — decisions without attributable authority; mitigate with maker-checker enforced server-side and non-repudiable audit. All three concentrate at trust boundaries: vendor, analysts, admin planes.',
   'Generic "we encrypt everything".',
   ['Which admin plane worries you most here?'],['d16','b5']],

  /* ---- Leadership / communication banking ---- */
  ['bk40',[BSA,BSO],'Leadership','Advanced','How would you explain architecture risk to compliance stakeholders?',
   'Spec-listed translation skill.','Their currency: control gaps and evidence → concrete scenario → options priced → written record.',
   [['Translate to control gaps and regulatory exposure','control','evidence','regulat','exposure'],['Concrete failure scenario, no jargon','scenario','concrete','example'],['Options with residual risk each','option','residual','priced'],['Written risk record with ownership','written','register','owner']],
   'Compliance thinks in controls and evidence, so translate: "today, a missed Kafka event means a customer change may never be screened — that is a control gap with no detective control; here is the scenario." Offer options with residual risk: reconciliation control (detective, 4 weeks) vs full outbox rework (preventive, 12 weeks). Put it in the risk register with an owner — compliance respects written, owned risk more than verbal assurance.',
   'Explaining consumer groups to the MLRO.',
   ['They ask "can this happen today?" — your honest answer format?'],['m7','b5']],
  ['bk41',[BSA,BSO],'Leadership','Advanced','How would you challenge a vendor recommendation pushed by senior management?',
   'Spec-listed political skill.','Steelman it → evidence-based evaluation on your scenarios → hidden costs → decision framework, not opposition.',
   [['Steelman before challenging','steelman','strongest','credit'],['Demand evidence on your scenarios: PoC, references','poc','reference','scenario','evidence'],['Surface hidden costs: integration, exit, customisation','integration','exit','hidden','tco'],['Offer criteria and process, not a counter-opinion','criteria','matrix','process']],
   'Never fight opinion with opinion. Steelman the vendor\'s case first — credibility depends on it. Then propose the process: a scripted PoC on our data shapes and failure scenarios, reference calls with banks our size, and a TCO view including integration, customisation debt and exit. Present the weighted matrix and let the evidence challenge the recommendation. If it survives honestly, support it — that was the point.',
   'Public opposition without an alternative process.',
   ['The PoC is refused for time reasons — next move?'],['a4','m6']],
  ['bk42',[BSA,EA],'Leadership','Expert','How do you manage architecture decisions across multiple banking teams?',
   'Spec-listed scale governance.','Decision rights per scope → contracts between teams → light ADR-based forum → guardrails absorb routine.',
   [['Explicit decision rights: local vs cross-team vs enterprise','decision rights','scope','who decides'],['Contracts (API/event/data) as the inter-team law','contract','between','interface'],['Fast ADR-reviewing forum for genuinely shared decisions','forum','adr','review','days'],['Guardrails/pipelines for the routine','guardrail','pipeline','automat']],
   'Define decision rights: teams decide inside their bounded context; contract changes between contexts need affected-team review; a small set (data classification, event backbone standards, security model) is enterprise-level. Inter-team law is the contract — schema-registry compatibility and contract tests enforce it mechanically. A weekly one-hour forum reviews cross-team ADRs with pre-reads; everything routine lives in guardrails. Decisions get made where the knowledge is, coherence comes from contracts and a few real standards.',
   'Every decision through one board (bottleneck) or none (fragmentation).',
   ['A team ships a breaking event change bypassing the process. Response?'],['m3','m5']],
  ['bk43',[BSA,BSO],'Leadership','Advanced','The business demands onboarding time cut from 5 days to 1, but screening SLAs are the bottleneck. Navigate.',
   'Business-pressure navigation with control integrity.','Decompose the 5 days honestly → what architecture can fix → policy options with risk owners → never silently weaken controls.',
   [['Measure where the 5 days actually go','measure','decompose','where'],['Architecture fixes: parallelise, pre-screen, automate doc checks','parallel','automat','pre-screen'],['Risk-owned policy options: provisional activation, risk-based tiers','provisional','risk-based','policy','tiered'],['Controls weakened only by explicit, owned risk acceptance','never silently','risk acceptance','compliance sign']],
   'First measure: usually most of the 5 days is queues and manual document checks, not screening compute. Architecture recovers time honestly — parallelise checks, automate document verification, event-driven screening in minutes. If a gap remains, the options are risk decisions for compliance to own: risk-based tiers (low-risk customers fast-tracked), provisional activation with restricted limits pending clearance. What I refuse: silently relaxing screening because a KPI demanded it.',
   'Promising the KPI and quietly widening thresholds.',
   ['Compliance rejects provisional activation. What remains?'],['b5','m7']],
  ['bk44',[BSA,BSO],'Leadership','Advanced','How would you define RTO and RPO for a critical banking system and defend them to a review board?',
   'Spec-listed resilience governance.','Derive from business impact per service → tier and price → honest async caveats → tested evidence.',
   [['Impact-based derivation per business service','impact','per hour','derive','business service'],['Tiered architecture priced per objective','tier','price','cost'],['Honest async-replication RPO statement','async','lag','honest','nonzero'],['Game-day evidence of achieved values','game day','tested','achieved','evidence']],
   'Per important business service (payments submission ≠ statement viewing): quantify an hour of outage and a minute of data loss with the business, derive RTO/RPO, and price the architecture tier each implies — the board sees cost per nine. State honestly that async replication means nonzero RPO with reconciliation owning the tail. Defend with evidence: game-day results showing achieved RTO/RPO against the promise. Boards trust measured numbers over aspirational ones.',
   'RTO 0 / RPO 0 on a slide, untested.',
   ['Your last game day missed the RTO by 12 minutes. What do you tell the board?'],['d5','b9']],

  /* ---- Banking system design ---- */
  ['bk45',[BSA,BSO],'System design','Expert','Design a customer onboarding platform.',
   'Spec-listed flagship design.','Workflow orchestration → KYC/screening integration → provisional policy → audit/evidence → failure paths.',
   [['Orchestrated long-running workflow with human steps','orchestrat','workflow','state','human'],['Identity verification + screening as integrated services','kyc','verify','screening','integrat'],['Business policy for waits: block vs provisional','provisional','block','policy'],['Full audit/evidence of every step and decision','audit','evidence','version'],['Failure paths: vendor outages, abandoned applications, retries','abandon','outage','resume','idempotent']],
   'An orchestrated workflow (engine or dedicated orchestrator — visibility matters: "where is application 123?") through capture → document/identity verification → screening → risk rating → approval (four-eyes above thresholds) → account opening in the core → activation. Vendors (ID verification, screening) integrate behind ACLs with breaker/queue patterns; waits follow business policy (provisional vs blocked). Every step audited with data versions; applications resumable and idempotent; abandoned flows expire with data-retention rules. Events (ApplicationSubmitted, ScreeningCompleted, AccountOpened) integrate CRM, notification and audit.',
   'A synchronous chain of 6 REST calls from a web form.',
   ['Where do you put the four-eyes step and why there?'],['cs10','b5']],
  ['bk46',[FCA,BSA],'System design','Expert','Design a transaction-monitoring platform.',
   'Behavioural-detection architecture.','Stream ingestion → rules+models detection → alert/case workflow → tuning governance → volumes.',
   [['Event-stream ingestion of posted transactions','stream','ingest','posted'],['Hybrid detection: rules + models, both versioned','rule','model','version','hybrid'],['Alert → case workflow with SAR path','alert','case','sar'],['Detection logic governance and backtesting','backtest','govern','tune'],['Volume/latency tiers: real-time vs batch scenarios','tier','real-time','batch']],
   'Posted transactions stream into a detection layer combining versioned rules (structuring thresholds, velocity) with scored models — both governed: every change backtested, approved, and reproducible ("which logic version raised this alert?"). Alerts dedupe/correlate into cases (the screening case-management pattern reused), with SAR filing workflows and tipping-off controls. Detection runs in tiers: near-real-time for high-risk patterns, daily batch for aggregation scenarios. Scale maths first: 10M tx/day is stream-friendly; a 90-day-window aggregation is a stateful-processing or warehouse job.',
   'One giant nightly SQL query per rule.',
   ['An analyst asks why alert 999 fired. What must the platform answer with?'],['cs12','b5']],
  ['bk47',[BSA,BDA],'System design','Expert','Design a customer master-data platform for a bank.',
   'MDM architecture judgement.','Golden-source strategy → match/merge governance → distribution via events → survivorship + audit.',
   [['One golden source; consumers via events/API','golden','master','distribute'],['Match/merge with human stewardship for low confidence','match','merge','steward','duplicate'],['Survivorship rules explicit and versioned','survivorship','precedence','rule'],['History, audit and GDPR overlay','history','audit','erasure']],
   'A party master as golden source: ingesting from channels and legacy sources, match/merge (deterministic + probabilistic) with data stewards deciding low-confidence merges — never silent auto-merges of customer identities. Survivorship rules (which source wins per attribute) explicit and versioned. Distribution via customer.events.v1 (outbox) and query APIs; consumers keep derived copies. Full history (regulators ask "what did we believe on date X"), merge/unmerge audit, and the GDPR/retention overlay per attribute class.',
   'A nightly deduplication script into a new table.',
   ['Two customers were wrongly merged and transacted for a month. Unwind it.'],['cs14','b7']],
  ['bk48',[BSA,BDA],'System design','Advanced','Design a regulatory-reporting platform.',
   'Point-in-time correctness engineering.','Versioned ingestion → lineage → reproducible calculation → deadline operations.',
   [['Versioned, point-in-time source snapshots','point-in-time','version','snapshot','as of'],['Field-level lineage to sources','lineage','trace'],['Reproducible runs: same inputs+logic = same report','reproduc','rerun','same'],['Deadline-aware operations: late-start alerts, reruns','deadline','alert','rerun','late']],
   'Reports must be reproducible: ingestion snapshots versioned inputs (or a bitemporal store answers "as known on date X"); calculation logic versioned like code; a rerun of report R for period P with logic v3 yields byte-identical output. Field-level lineage traces every figure to sources — a finding otherwise. Operations are deadline-shaped: dependency-graph scheduling, late-start (not just failure) alerts because the 06:00 submission is a compliance clock, and controlled resubmission workflows with difference audits.',
   'Excel exports from the warehouse, versioned by filename.',
   ['The regulator queries one figure from last quarter. Walk the trace.'],['cs16','b7']],
  ['bk49',[BSA,BSO],'System design','Expert','Design a multi-region banking system with data-residency restrictions.',
   'Spec-listed constraint-driven design.','Residency as partitioning driver → home-region writes → global thin layer → cross-border flows governed.',
   [['Customer home-region write partitioning','home','partition','pin','write'],['Thin global layer: directory/routing only, non-restricted data','directory','global','routing','thin'],['Cross-border flows explicit, minimal, legally reviewed','cross-border','transfer','legal','explicit'],['Per-region DR + honest global-view latency','per-region','dr','eventual','view']],
   'Residency drives the partitioning: each customer homed to their jurisdiction\'s region — data, processing, and often admin staff pinned there. A thin global layer carries only what may cross borders: a routing directory of non-restricted keys, aggregated non-personal metrics. Cross-border needs (group risk, global sanctions compliance) become explicit, minimised, legally-reviewed data flows — not accidental replication. DR is per-region (residency usually binds the DR site too), and any "global view" is eventually consistent by design and labelled so.',
   'Global active-active replication with residency filters bolted on.',
   ['Group-level fraud detection needs signals from all regions. Options?'],['cs21','dt2']],
  ['bk50',[BSA,BSO],'System design','Expert','Migrate a banking monolith to microservices — plan it.',
   'Spec-listed migration mastery.','Modularise first → strangle by domain with data ownership per slice → parallel-run recon → controls throughout.',
   [['Modularise in place; extract by bounded context','modular','in place','bounded context'],['First slice = learning value, not payments','first','low risk','notification','party'],['One writer per entity per phase + parallel-run reconciliation','writer','parallel run','reconcil'],['Regulatory continuity: audit and controls never gap','audit','control','continuity','regulator']],
   'Modularise the monolith along bounded contexts first — cheap to adjust while wrong. Extract via strangler facade, first slice chosen for learning (party lookups or notification, never payments), each slice with an explicit data-ownership decision and parallel-run reconciliation until clean. The banking overlay: audit trails and controls must never gap mid-migration — a regulator does not accept "we were between architectures". The ledger core may legitimately remain a modular monolith at the end; that is success, not incompletion.',
   'A 24-month big-bang with a hard cutover weekend.',
   ['Which slice never leaves the monolith, possibly forever?'],['m2','ms2']],

  /* ---- Failure & recovery banking ---- */
  ['bk51',[BSA,KA],'Failure & recovery','Advanced','A message sits in the screening dead-letter topic. Walk through the complete lifecycle.',
   'DLT operations discipline.','Alert → triage classification → fix → replay with idempotency → evidence closure.',
   [['Alert pages an owner immediately','alert','page','owner'],['Triage: poison vs transient vs bug classification','triage','classif','poison'],['Fix then controlled replay to source','replay','fix','controlled'],['Compliance evidence: the gap and repair documented','evidence','document','gap','audit']],
   'The DLT alert pages the owning team with the error context headers. Triage classifies: malformed event (producer bug — fix upstream, transform-replay), transient exhaust (replay as-is), or consumer defect (fix, deploy, replay). Replay goes back to the source topic through the standard idempotent path — never hand-processed around the controls. Closure includes compliance evidence: which parties\' screening was delayed by how long, because a screening gap is a reportable condition, not just an ops ticket.',
   'A weekly glance at DLT depth.',
   ['Why must replay flow through the normal consumer path?'],['k4','b5']],
  ['bk52',[PAY,BSA],'Failure & recovery','Expert','Mid-day, the payment orchestrator loses its database. Payments are in unknown states. Lead.',
   'Money-in-flight crisis leadership.','Stop intake cleanly → restore → state reconstruction from events/scheme → reconcile before resume → report.',
   [['Controlled intake stop with clean client errors','stop','intake','reject','clean'],['Restore with known RPO; identify the loss window','restore','rpo','window'],['Reconstruct in-flight states from outbox/scheme records','reconstruct','scheme','event','inquiry'],['Reconcile fully before resuming; no blind resubmission','reconcil','before resume','no duplicate'],['Incident comms + regulatory clocks','communic','regulat','report']],
   'Stop intake first — clean rejections beat unknown acceptance. Restore the database (the RPO window defines potentially lost state), then reconstruct: outbox/audit events, scheme status inquiries and core postings triangulate each in-flight payment\'s true state. Payments proven un-submitted are resubmitted idempotently; proven submitted are completed; ambiguous ones are resolved via scheme records before anything moves — blind resubmission is how you double-pay. Resume intake only after reconciliation closes, and the incident report (with customer/regulatory notifications per clocks) documents the evidence chain.',
   'Restarting the service and hoping retries sort it out.',
   ['Which designed features made this recovery possible at all?'],['b4','b9']],
  ['bk53',[BSA,FCA],'Failure & recovery','Advanced','Screening decisions for last week used a stale sanctions list. Respond.',
   'Compliance-incident response.','Scope the exposure → re-screen affected decisions → root cause list ingestion → disclosure posture.',
   [['Quantify: which decisions, which list delta','scope','which','delta','window'],['Re-screen against the correct list with priority','re-screen','priority','correct'],['Root cause: ingestion monitoring gap','ingestion','monitor','root cause','version'],['Compliance-led disclosure assessment + evidence','disclos','compliance','evidence','document']],
   'Scope first: decisions made in the window × the delta between stale and current lists — that intersection is the exposure, usually far smaller than "all of last week". Re-screen the affected set with priority on the list-delta matches; any new hits follow expedited case handling. Root cause the ingestion failure (why did list-version monitoring not fire?) and add the missing control. Disclosure to the regulator is compliance\'s call — the architecture\'s job is giving them exact evidence: what was screened against what, when, and the repair trail.',
   'Quietly re-screening everything and mentioning it to nobody.',
   ['What monitoring would have caught this on day one?'],['b5','b9']],
  ['bk54',[BSA],'Failure & recovery','Advanced','Month-end volumes are 4× normal and the platform is degrading. What did good design already do, and what do you do now?',
   'Peak-day operations.','Designed: capacity plan + degradation menu → now: shed by business priority → protect settlement-critical → learn.',
   [['Capacity planned to calendar peaks (this was foreseeable)','capacity','month-end','planned','foreseeable'],['Pre-agreed degradation menu activates','degrad','menu','pre-agreed'],['Shedding by business priority: payments before statements','priorit','shed','payments before'],['Post-event: capacity model corrected','learn','model','load test']],
   'Month-end is the most foreseeable peak in banking — good design already load-tested to it and pre-agreed the degradation ladder. Now: activate it in order — defer statement generation and non-critical batch, shed analytics traffic, protect the payment path and settlement-critical jobs with their bulkheaded capacity, communicate the ladder\'s state to operations and business. Afterwards, the capacity model gets corrected: a 4× month-end that surprised us is a planning defect, not a traffic anomaly.',
   'Emergency vertical scaling and prayer, monthly.',
   ['What belongs in the degradation ladder, and who signed it?'],['b9','d4']],

  /* ---- More banking depth (quick-fire spread) ---- */
  ['bk55',[BSA,BSO],'Banking domain','Intermediate','What is a mandate and why is it an architectural challenge?',
   'Entity-modelling depth.','Definition → rule complexity → enforcement point → audit linkage.',
   [['Operating authority rules on accounts','authority','operate','signatory'],['Combinatorial rules: joint, limits, roles','joint','limit','any two'],['Backend enforcement at payment release','enforce','release','check'],['Mandate version recorded in audit','version','audit']],
   '"Who may do what on this account": signatories, joint rules ("any two directors"), amount thresholds, delegation. The challenge: mandate rules are combinatorial and change over time, must be enforced at the moment of action (payment release checks the mandate *as of now*), and the mandate version used must be recorded in the audit trail — a payment approved under a mandate that had been revoked is a serious control failure.',
   'Modelling mandates as user-account role rows.',
   ['A mandate changes while a payment awaits its second approval. What happens?'],['b2','b3']],
  ['bk56',[FCA,BDA],'Banking domain','Advanced','Why is beneficial-ownership data graph-shaped, and what does that mean for your design?',
   'UBO data architecture.','Ownership chains → traversal queries → store choice → screening scope derivation.',
   [['Multi-hop ownership chains across entities','chain','multi-hop','layers'],['Traversal queries: effective ownership %','traverse','effective','percentage'],['Graph store or recursive relational — justified either way','graph database','recursive','cte'],['Screening scope derives from traversal','scope','derive','screen']],
   'UBO means walking ownership chains — A owns 60% of B which owns 50% of C — to find natural persons above control thresholds: inherently multi-hop traversal with computed effective percentages. Design options: a graph database where traversal dominates, or recursive SQL on the relationship domain when volumes are modest — justify either. The output matters: screening scope for an organisation derives from this traversal, so its correctness and freshness are compliance properties, and every scope computation should be reproducible as evidence.',
   'A flat owner column on the customer table.',
   ['The trust in the chain refuses to disclose its controllers. System behaviour?'],['b2','dt1']],
  ['bk57',[BSA,IA],'Banking domain','Intermediate','A corporate client submits payment files. Design the ingestion properly.',
   'File-integration discipline.','Transport + checksums → duplicate-file defence → record validation/partial handling → recon + status.',
   [['Integrity: checksums, completeness markers','checksum','complete','integrity'],['Duplicate-file detection (hash/business window)','duplicate file','hash','resubmi'],['Per-record validation with partial-failure policy','partial','record','reject policy'],['Acknowledgement, status reporting and reconciliation','status','acknowledge','reconcil']],
   'SFTP/API drop with checksums and completeness markers (never process a file mid-transfer). Duplicate-file defence: content hash plus business-level dedupe — clients re-send files, always. Per-record validation with an agreed partial-failure policy (reject file vs reject records — contractual, not improvised). Every file gets an acknowledgement and per-record status reporting; a reconciliation ties records-received to payments-processed. This unglamorous pipeline moves more money than most APIs.',
   'Parsing whatever lands in the folder, immediately.',
   ['The same file arrives twice with one changed record. Behaviour?'],['a6','b4']],
  ['bk58',[BSA,KA],'Kafka','Intermediate','Which banking events belong on a compacted topic vs a retention topic?',
   'Topic-semantics matching.','Changelog vs history distinction → examples each way → tombstones/GDPR → audit exclusion.',
   [['Compacted = latest state per key (changelog)','compact','latest','changelog'],['Retention = history/flow (facts over time)','retention','history','flow'],['Examples: customer-state vs payment events','customer state','payment','example'],['Audit needs neither — immutable store; tombstones for erasure','audit store','tombstone','erasure']],
   'Compaction fits changelogs — "current state per key": customer-current-state topics rebuilding caches and read models; tombstones double as the GDPR-erasure lever. Time-retention fits flows — payment lifecycle events, screening requests — where history within the window matters. Neither is an audit store: long-term evidence sinks to an immutable store with its own retention regime. Saying that unprompted separates architects from users.',
   'Compacting a payment-events topic.',
   ['Rebuild a customer cache: which topic type and why?'],['k1','b7']],
  ['bk59',[BSA,JA],'Java & Spring','Intermediate','Where do correlation and audit IDs come from and how do they flow through a banking stack?',
   'Traceability plumbing.','Edge generation/acceptance → MDC + headers propagation → Kafka headers → audit linkage.',
   [['Generated/accepted at the edge, returned to callers','edge','generate','return'],['MDC + HTTP headers through sync hops','mdc','header','propagat'],['Kafka headers across async hops','kafka header','async','event'],['Audit records reference the same IDs','audit','link','reference']],
   'The gateway accepts or mints the correlation ID, returns it to the caller, and every hop propagates it — MDC into logs, HTTP headers across sync calls, Kafka headers across async ones (including through the outbox: the relay copies them). Audit events carry the same correlation plus a business audit ID, so "show me everything about payment X" joins logs, traces and audit evidence into one story. The async propagation is where implementations quietly break — test it.',
   'Each service generating its own request ID.',
   ['Where does the correlation ID most often get lost?'],['a9','j4']],
  ['bk60',[BSA,BSO],'Trade-offs','Advanced','Vendor screening engine vs building matching in-house on Elasticsearch — argue it.',
   'Applied sourcing in fincrime.','Wardley placement → regulatory/model-governance burden → TCO both ways → common hybrid.',
   [['Matching quality is regulated, audited capability','regulat','audit','proven','model'],['Build = you own false-positive rates and governance','own','false positive','tuning','governance'],['Vendor costs: licence, lock-in, integration, exit','lock-in','licence','exit'],['Hybrid: vendor engine + owned orchestration/case layer','hybrid','orchestrat','around']],
   'Matching engines look buildable — fuzzy search on Elasticsearch — but the product is regulated matching *quality*: list coverage, algorithm validation, model governance, and answering the regulator\'s "prove your matching works". Vendors amortise that across clients; building means owning it alone. TCO both ways: vendor licence + integration + exit vs build + permanent tuning team + compliance validation. The common defensible answer: buy the matching engine, build the orchestration, workflow and evidence layers around it behind an ACL — differentiation lives there, not in fuzzy matching.',
   '"Elasticsearch can do fuzzy matching, so build."',
   ['What must the contract say about list updates and SLAs?'],['a5','b5']],
  ['bk61',[BSA,BSO],'Introduction','Beginner','Why banking technology? What keeps architects engaged in this domain?',
   'Motivation and domain realism.','Honest motivators → domain-specific challenges → realism about constraints.',
   [['Concrete technical draw: consistency, audit, scale under constraint','consistency','audit','constraint','correctness'],['Stakes and impact framing','money','trust','impact'],['Realism: regulation as design input, not annoyance','regulation','constraint','input']],
   'The honest draw: banking is where distributed-systems theory has teeth — exactly-once effect, reconciliation and auditability are not interview trivia but daily requirements with regulators reading the homework. The constraint density (residency, secrecy, calendars, controls) makes design genuinely interesting, and treating regulation as a design input rather than an obstacle is the mindset shift that separates engaged banking architects from frustrated ones.',
   '"The salaries."',
   ['Which banking constraint changed your design thinking most?'],['b1','b3']],
  ['bk62',[BSA,BSO],'Experience','Intermediate','Describe a banking-specific incident you learned the most from.',
   'Domain war-story authenticity.','STAR with banking specifics → control angle → durable fix.',
   [['Concrete banking scenario (recon break, duplicate posting, missed event)','recon','duplicate','missed','break'],['Business/regulatory impact quantified','impact','customer','regulat'],['Control gap identified, not just bug fixed','control','gap','detective'],['Durable fix in process and architecture','fitness','monitor','since']],
   'Strong answers feature: a reconciliation break or duplicate-processing event, the business impact in customer/regulatory terms, and — crucially — the recognition that a control was missing, not just a bug present: "the defect was ordinary; the finding was that nothing would have detected it. We added the reconciliation and a fitness check." Interviewers listen for control-thinking over hero-debugging.',
   'A generic outage story with banking nouns inserted.',
   ['What detective control exists now that did not then?'],['b7','b9']],
  ['bk63',[BSA,MA],'Microservices','Advanced','Which banking domains tolerate eventual consistency and which never do?',
   'Consistency placement judgement.','Never: balance/posting integrity → tolerates: propagation flows → grey zone with policy → detection everywhere.',
   [['Posting/reservation integrity: strongly consistent','posting','reservation','strong','never'],['Propagation (CRM views, search, notifications): eventual fine','crm','search','propagat','fine'],['Grey zone needs explicit policy: screening scope, mandate views','grey','policy','screening scope','mandate'],['Staleness windows monitored, not assumed','window','monitor','lag']],
   'Never eventual: the invariant core — balance + reservation + posting must be transactionally consistent (double-spend is not a lag, it is a loss). Comfortably eventual: propagation — CRM copies, search indices, dashboards, notifications, with monitored lag. The grey zone needs explicit policy: how stale may a mandate view or screening scope be before action? Those windows are business risk decisions, written down, with staleness monitored — "eventual" without a bound and a monitor is negligence in this domain.',
   'Either "everything must be strongly consistent" or "eventual is fine everywhere".',
   ['Put a number on acceptable mandate-view staleness and defend it.'],['b6','d1']],
  ['bk64',[BSA,SEC],'Security','Intermediate','Design logging for a payment service that satisfies both engineers and auditors.',
   'Dual-purpose logging design.','Two artefact types → structured logs with allowlist → audit events separately → linkage.',
   [['Operational logs ≠ audit records: two artefacts','separate','two','different guarantee'],['Structured, correlation-ID-carrying, PII-allowlisted logs','structured','correlation','allowlist'],['Audit events: immutable, complete, retained','immutable','retention','complete'],['Linked by IDs for end-to-end stories','link','join','story']],
   'Two deliberately different artefacts: operational logs — structured JSON, correlation IDs, PII-allowlisted fields, weeks of retention, optimised for debugging; and audit events — business facts with actor/authority/versions, immutable, years of retention, optimised for evidence. Never conflate them: logs get rotated and are mutable in practice; audit must be neither. They join via correlation/audit IDs so an investigation reads one story across both.',
   'One log stream doing both jobs badly.',
   ['Why can\'t high-quality logs simply serve as the audit trail?'],['b3','a9']],
  ['bk65',[BSA,BSO],'Summary','Intermediate','Summarise your screening-platform design for the panel in five sentences.',
   'Banking design compression.','Problem/drivers → shape → two control-flavoured trade-offs → risk + validation.',
   [['Drivers named: compliance SLAs, evidence, resilience','driver','evidence','sla'],['Shape: event-driven core, vendor behind ACL, case workflow','event-driven','vendor','case'],['Trade-offs with control framing (outbox for no-lost-events)','outbox','trade-off','control'],['Top risk + next validation step','risk','validate','poc']],
   '"The platform screens customers and partners on data changes, list updates and onboarding, with full decision evidence. It is event-driven — party events in, outbox-guaranteed requests out to a vendor engine behind an anti-corruption layer, cases managed idempotently with four-eyes release. I chose outbox+inbox+reconciliation over simpler direct publishing because a lost screening event is a control gap, not a bug. The main risk is vendor latency at list-update peaks; a two-day throughput PoC retires it. Evidence retention and replay were designed in, not appended."',
   'Re-narrating every component for five minutes.',
   ['Now defend the vendor choice in one sentence.'],['b5','m7']]
  ];

  D.interviewQs = (D.interviewQs || []).concat(Q.map(function (q) {
    return { id: q[0], role: q[1], cat: q[2], difficulty: q[3], q: q[4], testing: q[5], structure: q[6],
      kp: q[7], strong: q[8], weak: q[9], followups: q[10], lessons: q[11], banking: true };
  }));
})(window.SAA.data);
